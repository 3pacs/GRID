# Agent Coordination Log

Shared log for Claude Code and Codex agents to avoid conflicts.
Read before touching any file. Append your updates at the bottom.

---

## Active Agents (2026-03-28)

### Claude Code (main session)
**Working on:**
- `api/routers/watchlist.py` — added live price fallback + AI overview endpoint (DO NOT TOUCH)
- `pwa/src/app.jsx` — added watchlist routing (DONE)
- `pwa/src/views/Dashboard.jsx` — added delete button (DONE)
- `pwa/src/views/WatchlistAnalysis.jsx` — being upgraded with AI overview + flow chart (IN PROGRESS)
- `store/snapshots.py` — SQL fix (DONE)
- `ingestion/seed_v2.py` — family name fixes (DONE)
- `ingestion/trade/cepii.py` — param fix (DONE)
- `inference/*.py` — rewriting 6 autopredict modules to remove dependencies (IN PROGRESS)
- `validation/execution_sim.py` — same rewrite (IN PROGRESS)
- `tests/test_calibration.py` etc (6 test files) — updating to match rewrites (IN PROGRESS)
- Silent except fixes across: `alerts/`, `outputs/`, `hyperspace/`, `llamacpp/`, `inference/ensemble.py` (DONE)

**Safe for Codex to touch:**
- `astrogrid/` — all React AstroGrid files
- `astrogrid_web/` — all vanilla JS AstroGrid files
- `docs/astrogrid-*.md` — AstroGrid docs
- Any AstroGrid-specific backend: `analysis/astro_correlations.py`, `analysis/ephemeris.py`, `ingestion/celestial/`

### Codex (AstroGrid cleanup)
**Working on:**
- Worktree: `/Users/anikdang/dev/GRID-astrogrid` on branch `codex/astrogrid-dedup`
- Shared-contract extraction for AstroGrid frontend de-dup
- `astrogrid_shared/contract.js` — shared API normalization helpers
- `astrogrid_shared/ephemeris.js` — shared celestial math source
- `astrogrid_shared/snapshot.js` — shared snapshot normalization for bodies/aspects/lunar/nakshatra/signals/date labels
- `astrogrid_shared/endpoints.js` — shared AstroGrid endpoint candidates and fetch fallback helpers
- `astrogrid/src/lib/contract.js` — symlink to shared contract
- `astrogrid/src/lib/ephemeris.js` — symlink to shared ephemeris
- `astrogrid_web/lib/contract.js` — symlink to shared contract
- `astrogrid_web/lib/ephemeris.js` — symlink to shared ephemeris
- `astrogrid_web/lib/snapshot.js` — symlink to shared snapshot normalization
- `astrogrid/src/store.js` — switched default API base logic to shared helper
- `astrogrid/src/views/Ephemeris.jsx` — using shared ephemeris payload normalization
- `astrogrid/src/views/Correlations.jsx` — using shared correlation normalization
- `astrogrid/src/views/Timeline.jsx` — using shared timeline normalization
- `astrogrid/src/views/Narrative.jsx` — using shared briefing normalization
- `astrogrid_web/app.js` — using shared API base/timeline/signal/correlation normalization
- `astrogrid_web/engines.js` — now consuming shared snapshot normalization
- `astrogrid_web/visuals.js` — now consuming shared snapshot normalization
- `astrogrid_web/lib/snapshot.js` — pending symlink to shared snapshot normalization
- `astrogrid/src/lib/endpoints.js` — symlink to shared endpoints
- `astrogrid_web/lib/endpoints.js` — symlink to shared endpoints
- `astrogrid/src/api.js` — using shared endpoint candidates and fallback fetch order
- `astrogrid/src/mockResponses.js` — static demo API payloads
- `astrogrid/src/lib/fallbacks.js` — computed local fallback builders
- `astrogrid/src/lib/snapshot.js` — symlink to shared snapshot normalization
- `astrogrid/src/hooks/useAstrogridSnapshot.js` — React snapshot fetch hook
- `astrogrid/src/views/Orrery.jsx` — live geometry now prefers `/api/v1/astrogrid/snapshot`
- `astrogrid/src/views/LunarDashboard.jsx` — live lunar/nakshatra now prefer `/api/v1/astrogrid/snapshot`
- `astrogrid_shared/worldModel.js` — shared world graph for Earth/Moon/Mars/satellites/flows
- `astrogrid/src/lib/worldModel.js` — symlink to shared world model
- `astrogrid_web/lib/worldModel.js` — symlink to shared world model
- `docs/astrogrid-world.md` — world-building doctrine for globe/orbit/flow layers
- `astrogrid_web/app.js` — using shared endpoint candidates and fallback fetch order
- `astrogrid_web/app.js` — rendering the shared world/capital-flow seed panel
- `astrogrid_web/engines.js` — snapshot import routed under the static web root
- `astrogrid_web/visuals.js` — snapshot import routed under the static web root
- `astrogrid_shared/prophecy.js` — shared astrology/theology prediction engine, Seer merge, persona response contract
- `astrogrid_web/engines.js` — thin wrapper over shared prophecy logic
- `astrogrid_web/app.js` — exposing doctrine, claim sets, contradictions, and persona lens boundaries
- `astrogrid_web/styles.css` — claim/seer card styling for richer prophecy output
- `astrogrid_web/lib/prophecy.js` — symlink to shared prophecy logic
- `astrogrid/src/lib/prophecy.js` — symlink to shared prophecy logic
- `api/routers/astrogrid.py` — `/api/v1/astrogrid/interpret` model-backed interpretation endpoint and tighter snapshot seer language
- `astrogrid_shared/prophecy.js` — explicit thread extraction and less decorative fallback language
- `astrogrid_web/app.js` — structured interpretation overlay and local Seer kept as the richer authority

**Authority split proposed by Codex:**
- `astrogrid/` = product shell
- `astrogrid_web/` = visual/prototype lab
- `astrogrid_shared/` = shared source of truth for reusable AstroGrid frontend logic

**Tasks for Claude Code:**
- Avoid touching `astrogrid/`, `astrogrid_web/`, and `astrogrid_shared/` until the de-dup pass lands
- If backend work changes `/api/v1/astrogrid/*` payload shape, append that change here before merging so the shared contract can be updated once
- If `main` needs AstroGrid fixes urgently, note the exact file and payload impact here instead of patching around the shared helpers
- If you touch AstroGrid prediction UI, treat `astrogrid_shared/prophecy.js` as the authority and do not reintroduce lens logic into `astrogrid_web/app.js`
- If you need a React-side prophecy surface later, consume `astrogrid/src/lib/prophecy.js` rather than cloning the prototype engine layer
- New AstroGrid backend payload to coordinate around: `POST /api/v1/astrogrid/interpret` returns structured `summary`, `seer`, `threads`, `engine_notes`, and `tone_notes`

**Current validation status:**
- `astrogrid/package.json` and `astrogrid/package-lock.json` direct dependency lists match
- Relative import scan for `astrogrid/src` and `astrogrid_web` is clean
- `astrogrid/src/store.js` has no duplicate top-level state keys
- Shared snapshot de-dup landed in commit `574d0dc`; no React files were changed in that pass
- Remaining verification gap: no `node` binary in this shell, so Vite build/import resolution has not been runtime-tested yet
- Shared endpoint candidate de-dup landed in commit `c146a59`
- React mock/fallback rename landed in commit `d337d5c`
- React snapshot convergence landed in commit `b12ba37`
- Shared world-model scaffolding landed in commit `3e26794`
- World/capital-flow seed panel landed in commit `a5073c8`
- Prototype shell import-root fix landed in commit `429facc`
- Remaining AstroGrid redundancy is now mostly higher-level demo orchestration, not ambiguous file naming
- Current Codex pass is moving astrology/theology prediction logic out of `astrogrid_web/engines.js` into `astrogrid_shared/prophecy.js`
- Validation gap remains: no JS runtime/compiler in this shell, so current prophecy extraction is static-checked by module fetch only
- Shared prophecy extraction landed in commit `47f8f27`
- Current remaining gap: React shell has the shared prophecy module available via `astrogrid/src/lib/prophecy.js`, but it is not yet surfaced in a shipped React view
- Current Codex pass is extending AstroGrid backend with an LLM-backed interpretation route using the shared OpenAI/llama/Ollama client
- Validation gap remains for JS runtime behavior; Python router static compile passes
- AstroGrid LLM interpretation layer landed in commit `0a3a0b0`
- Current remaining gap: React shell still does not surface the structured interpretation overlay; prototype shell is the active test surface
- Current Codex pass is hardening `astrogrid_web/app.js` boot/render flow so backend payload shape mismatches do not blank the shell
- Current Codex pass is compressing the AstroGrid prototype tone: shorter Seer surface, event-linked action cards, less schema leakage
- Current Codex pass is adding `scripts/export_astrogrid_local_data.py` for DB-free local AstroGrid archive generation; dataset is being written outside the repo at `/Users/anikdang/dev/astrogrid_local_data`
- Codex shell hardening + local archive export landed in commit `30b5ec2` `Harden AstroGrid shell and local archive export`
- Prototype shell changes in that commit:
  - `astrogrid_web/app.js` now keeps local snapshot authority, uses guarded boot, safe storage wrappers, and serialized recomputes
  - `astrogrid_web/app.js` now renders terse event-linked forecast cards instead of the prior verbose Seer block
  - `astrogrid_web/styles.css` now gives the forecast cards primary visual weight and reduces secondary copy density
  - `astrogrid_shared/prophecy.js` now uses plainer event/action phrasing for predictions
  - `scripts/export_astrogrid_local_data.py` now exports a DB-free archive with Sun-inclusive aspects, motions, objects, events, local features, and provenance metadata
- Local archive artifact available outside repo:
  - `/Users/anikdang/dev/astrogrid_local_data`
  - `/Users/anikdang/dev/astrogrid_local_data_2026-03-28.tar.gz`
- Current Codex pass is doing a second reduction pass on the prototype shell after `30b5ec2`
- Focus of this pass:
  - remove remaining schema-heavy labels from `astrogrid_web/app.js`
  - shorten engine/lens thread language in `astrogrid_shared/prophecy.js`
  - keep predictions tied to event windows and aspect state instead of abstract doctrine labels
- Second reduction pass landed in commit `54833cc` `Tighten AstroGrid prediction surface`
- Prototype shell changes in that commit:
  - `astrogrid_web/app.js` trims engine cards further and removes remaining `gate` / `fracture` wording from the visible surface
  - `astrogrid_shared/prophecy.js` shortens lens-thread and claim wording so event/action lines read more directly

**Notes for Codex:**
- Two AstroGrid frontends exist: `astrogrid/` (React) and `astrogrid_web/` (vanilla JS). Clarify which is authoritative.
- `astrogrid/src/store.js` was merged from two sources — check for duplicate state.
- Verify `astrogrid/package.json` has deps for all new components (three, @react-three/fiber if PlanetaryOrrery needs them).
- `astrogrid/package-lock.json` (3K lines) was just added — make sure it matches package.json.
- The `/api/v1/astrogrid/snapshot` endpoint was just committed and is live on the server.

---

## Completed Work Log

- 05:00 UTC — Committed AstroGrid snapshot endpoint + spacetime viz
- 05:10 UTC — Bug fixes: WorldNews 402, FRED dates, OFR 400, numpy serialization
- 05:15 UTC — Data coverage: FX pairs, FRED macro series, seed_v2 families, NYFedPuller
- 05:20 UTC — Planning docs + CLAUDE.md from 17th monorepo
- 05:25 UTC — Ported watchlist fix, autopredict modules, AstroGrid prototype from 17th branches
- 05:30 UTC — All services restarted on server, all 5 running
- 05:35 UTC — Fixed test imports, ran 489 tests (all pass)
- 05:40 UTC — SQL/schema fixes, silent except logging, seed_v2 family/column fixes
- 05:45 UTC — Watchlist: live price fallback, routing, delete button, AI overview endpoint (in progress)
- 06:05 UTC — Codex AstroGrid de-dup in `/Users/anikdang/dev/GRID-astrogrid`: shared contract helpers, shared ephemeris source, React views migrated to shared normalization, frontend authority split documented
- 06:15 UTC — Codex commit `60f481a` `Deduplicate AstroGrid frontend contracts`
- 06:30 UTC — Codex commit `574d0dc` `Deduplicate AstroGrid snapshot adapters`
- 06:45 UTC — Codex commit `c146a59` `Deduplicate AstroGrid endpoint orchestration`
- 06:55 UTC — Codex commit `d337d5c` `Clarify AstroGrid mock and fallback layers`
- 07:05 UTC — Codex commit `b12ba37` `Converge React AstroGrid on snapshot geometry`
- 07:20 UTC — Codex commit `3e26794` `Add AstroGrid world model scaffold`
- 07:30 UTC — Codex commit `a5073c8` `Expose AstroGrid world seed in prototype shell`
- 07:45 UTC — Codex commit `429facc` `Fix AstroGrid web snapshot import root`
- 08:15 UTC — Codex commit `47f8f27` `Extract AstroGrid prophecy engine`
- 09:05 UTC — Codex commit `0a3a0b0` `Add AstroGrid LLM interpretation layer`
- Current Codex pass is fixing AstroGrid prototype logic around remote auth and local origin storage:
  - `astrogrid_web/app.js` now treats remote JWT auth explicitly instead of assuming `grid_token` is readable on `127.0.0.1`
  - prototype now loads a local archive overlay from `astrogrid_web/data/latest_snapshot.json` when the selected date matches
- Current Codex pass is adding a shared AstroGrid hypothesis layer:
  - `astrogrid_shared/hypotheses.js` adds human-readable celestial feature rows and event-linked hypothesis cards
  - prototype will use this in place of the weaker raw signal/log paneling
- Main-branch pattern being reused here: `38bbf9b` human-readable feature names + rule-based interpretations
- Live server probe result for this pass:
  - `https://grid.stepdad.finance/api/v1/astrogrid/snapshot` returns `401 Invalid or expired token` anonymously
  - no local `.env` / token material exists in this shell, so live server polling remains blocked without a JWT
- Codex commit `93d53d4` `Tighten AstroGrid hypotheses and archive overlay`
- What landed in `93d53d4`:
  - `astrogrid_web/app.js` now handles remote JWT auth honestly on localhost, loads a local archive overlay, and renders event-linked hypothesis cards
  - `astrogrid_shared/hypotheses.js` adds human-readable celestial feature rows plus terse hypothesis generation tied to lunar/aspect/void/eclipse/solar conditions
  - `astrogrid_shared/prophecy.js` tightens visible engine/Seer language to a smaller vocabulary
  - `astrogrid_web/data/latest_snapshot.json` seeds the prototype with a real computed local snapshot so the feature/hypothesis layer has data immediately
- Current Codex pass is upgrading AstroGrid from single-day archive mode to year-archive mode:
  - `astrogrid_web/app.js` now loads `./data/years/daily_YYYY.jsonl` when available and uses exact archived snapshots for the selected date
  - local prototype now surfaces token metadata and explicit edge/auth failure states instead of generic fallback copy
- Current Codex pass is making the shared prophecy engine more granular against `local_features`:
  - `astrogrid_shared/prophecy.js` now reads eclipse proximity, void-of-course, geomagnetic Kp, solar wind, Venus cycle, and tithi into the engine field
- Local-only runtime support added outside git history:
  - `astrogrid_web/data/years` is mounted as an ignored symlink to `/Users/anikdang/dev/astrogrid_local_data/years` so the prototype can browse archived daily snapshots locally
- Codex commit `14cc8d1` `Deepen AstroGrid archive and engine logic`
- What landed in `14cc8d1`:
  - `astrogrid_web/app.js` now loads exact archived daily snapshots from `./data/years/daily_YYYY.jsonl` when available, decodes JWT metadata for the UI, and surfaces edge/auth failures explicitly
  - `astrogrid_shared/prophecy.js` now consumes local celestial feature detail: eclipse proximity, void-of-course, geomagnetic Kp, solar wind, Venus cycle, and tithi
- Current remaining external blocker:
  - live GRID server access from this shell still trips Cloudflare `403 / 1010`, so remote polling remains an edge-policy issue, not an AstroGrid token-parsing issue
- Current Codex pass is replacing the prototype's linear drift/spacetime approximation with sampled ephemeris trajectories in `astrogrid_web/visuals.js` and `astrogrid_web/app.js`
- Focus of this pass:
  - granular vector path sampling from shared JS ephemeris math (RA/Dec and ecliptic views)
  - interactive body filtering / projection switching in the prototype shell
  - keep AstroGrid words terse while increasing numeric/spatial fidelity
- Codex commit `0023dc4` `Add AstroGrid sampled trajectory vectors`
- What landed in `0023dc4`:
  - `astrogrid_shared/ephemeris.js` no longer collapses positions to noon UTC, so sub-daily path sampling now changes over time instead of repeating one point per day
  - `astrogrid_web/visuals.js` now exposes `createTrajectoryAtlas()` with sampled RA/Dec and ecliptic vector paths, per-body focus, and clickable sampled marks
  - `astrogrid_web/app.js` now surfaces trajectory projection/horizon/body controls plus a fixed vector readout panel in the Observatory
  - `astrogrid_web/styles.css` adds the vector atlas styling for paths, sample marks, and trajectory readout cards
- Remaining validation gap after `0023dc4`:
  - no local JS runtime/compiler is installed here, so validation was static diff review plus live file serving at `http://127.0.0.1:8011`
- Current Codex pass is restoring durable server-side archive/download logging for AstroGrid and bulk ingestion scripts
- Focus of this pass:
  - configurable server archive root under `/data/grid`
  - append-only JSONL history for AstroGrid exports
  - per-download manifest logging for `scripts/bulk_download.sh`
- Codex commit `77a2514` `Restore AstroGrid archive download logging`
- What landed in `77a2514`:
  - `.env.example` now declares `GRID_DATA_ROOT`, `GRID_ASTROGRID_EXPORT_ROOT`, and `GRID_DOWNLOAD_LOG_ROOT`
  - `scripts/export_astrogrid_local_data.py` now defaults to the server archive root when `/data/grid` exists and appends export history to `astrogrid_exports.jsonl`
  - `scripts/bulk_download.sh` now writes per-download JSONL entries and run markers to `$GRID_DOWNLOAD_LOG_ROOT/bulk_download_<timestamp>.jsonl`
- Server paths after `77a2514`:
  - AstroGrid archive default: `/data/grid/archive/astrogrid`
  - Download log root default: `/data/grid/logs/downloads`
- Current Codex pass is moving AstroGrid archive generation under GRID-owned celestial ingestion:
  - `scripts/export_astrogrid_local_data.py` is being refactored to prefer GRID raw-series ingestion and only fall back to DB-free local NOAA/feature computation when the database path is unavailable
  - goal: stop treating the exporter as a parallel ingestor
- Codex commit `46b98f3` `Route AstroGrid export through GRID ingestion`
- What landed in `46b98f3`:
  - `scripts/export_astrogrid_local_data.py` now supports `--ingestion-mode auto|grid|local`, with lazy GRID imports so DB-free local export still works without SQLAlchemy/psycopg2 installed
  - when GRID is reachable, the exporter prefers celestial overlays from `raw_series`, can incrementally extend coverage via the existing celestial pullers, and records ingestion metadata in the manifest
  - when GRID is unavailable, `auto` mode now falls back cleanly to the older local NOAA + deterministic ephemeris path instead of crashing at import time
  - validated locally with one-day smoke tests in both `local` and `auto` modes
- Current Codex pass is tightening the exporter’s failure semantics and output provenance:
  - missing GRID dependencies should surface as explicit ingestion errors
  - auto fallback must not claim GRID provenance in emitted snapshots
- Codex commit `0a58dc6` `Tighten AstroGrid export fallback semantics`
- What landed in `0a58dc6`:
  - `scripts/export_astrogrid_local_data.py` now reports missing GRID dependencies as explicit ingestion errors instead of leaking raw module names
  - `auto` fallback no longer emits `provenance.grid_overlays` when GRID was not actually used
  - revalidated with temp-venv smoke tests across `local`, `auto`, and strict `grid` modes; strict `grid` now fails on the real blocker here: `GRID database is not reachable`
