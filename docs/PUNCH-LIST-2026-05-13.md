# GRID Punch List — 2026-05-13

This file is an append-only feed produced by the GRID Code Auditor for the
GRID Backlog Orchestrator to pick up via its TIER 4 path. Each item is a
single-PR-sized task (< 200 LOC, < 5 files).

Priority legend:
- `[P0]` — security / correctness bug actively affecting production. Next pick.
- `[P1]` — correctness or coverage gap likely affecting accuracy/reliability.
- `[P2]` — hygiene: dead code, long functions, stale TODOs, missing types.

---

## Auditor 2026-05-13 — oracle/

Metrics: 29 py files, 10,651 total LOC, 3 commits in last 30d.
Checks run: A B C D E F G H I J

> **2026-06-09 reconciliation note:** 11 of the 13 items in this section landed
> via the hermes-operator commit `8de99c4e` (2026-05-30) and are marked `[x]`
> below. The two remaining open items are `[P2]` engine-refactor splits that
> are too large for a single routine PR (`oracle/engine.py` is 2,877 LOC).

- [x] [P0] De-duplicate horizon helpers in `engine.py` (one canonical copy; remove the other) — `oracle/engine.py:136` and `oracle/engine.py:2158` — `_default_horizon_buckets` / `_parse_horizon_buckets` / `_horizon_key` are defined twice in the same module with divergent bodies (second `_horizon_key` adds a `canonical[days]` shortcut absent in the first); Python's module-load order means the second silently wins, masking the first definition's logic. **RESOLVED 2026-05-30 (commit 8de99c4e):** single canonical definitions remain at `oracle/engine.py:137,152,185`; the second copy was removed.
- [x] [P0] Collapse the two `publish_astrogrid_prediction` implementations to a single source of truth — `oracle/publish.py:51` and `oracle/publisher_gate.py:195` — `publish.py` enriches signals via `build_prediction_context` + `enrich_signals_payload` (regime / fci_regime / vix_level / signal_contributions), but `publisher_gate.py` does the un-enriched insert; `api/routers/oracle.py:18` calls the enriched version while `api/routers/astrogrid_helpers.py:77` calls the un-enriched one, so astrogrid predictions written through `astrogrid_helpers` skip the conviction-stack context the calibrators need. **RESOLVED 2026-05-30 (commit 8de99c4e):** `oracle/publisher_gate.py` is now a 161-line re-export shim that imports `publish_astrogrid_prediction` from `oracle.publish` (single enriched source of truth).
- [x] [P1] Rename or merge the duplicate `CalibrationReport` dataclass — `oracle/calibration.py:34` and `inference/calibration.py:57` — same class name, different field shapes across two modules; an accidental cross-import will type-check but produce wrong attribute access at runtime. **RESOLVED 2026-05-30 (commit 8de99c4e):** `oracle/calibration.py:34` renamed to `OracleCalibrationReport`; collision-prevention regression test at `tests/test_oracle_calibration_report_naming.py`.
- [x] [P1] Add unit tests for the chat publishing firewall pipeline — `oracle/firewall.py:42` — `verify_output` is the single entry point in `api/routers/chat.py:1409` (LLM output gate) and has zero tests; covers claim extraction → verification → sanity → gate → audit write. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_firewall.py` (473 LOC, 27 test cases) covers `verify_output`.
- [x] [P1] Add unit tests for `gate_decision` publish/review/reject thresholds — `oracle/publisher_gate.py:42` — feeds both chat firewall and astrogrid publish path, no tests covering the auto-publish (>0.85 confidence), reject (contradicted/critical-fail), and review (>30% flagged) branches. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_publisher_gate.py` (250 LOC, 22 test cases) covers `gate_decision`.
- [x] [P1] Add unit tests for `oracle/claim_extractor.py` price/percent/direction regexes — `oracle/claim_extractor.py:127` — `extract_claims` parses LLM text into structured claims for the firewall; zero tests, regex changes could silently drop or mis-tag claims. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_claim_extractor.py` (229 LOC, 19 test cases) covers `extract_claims`.
- [x] [P1] Add unit tests for `oracle/claim_verifier.py` DB-evidence verdicts — `oracle/claim_verifier.py:17` — verifies parsed claims against the live store; supports the supported / contradicted / insufficient verdict path used by `gate_decision`. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_claim_verifier.py` (380 LOC, 34 test cases) covers `verify_claim` verdict branches.
- [x] [P1] Add unit tests for `oracle/sanity_checker.py` deterministic checks — `oracle/sanity_checker.py:260` — `run_sanity_checks` runs price-range / pct-math / direction-consistency / date / unit / cross-claim checks; zero coverage despite gating publish decisions. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_sanity_checker.py` (367 LOC, 41 test cases) covers `run_sanity_checks`.
- [x] [P1] Fix Signal positional-arg mismatch in `_gather_signals_from_registry` — `oracle/engine.py:812` — `Signal(name, family, z, 0, sig_dir, conf, 0)` stores the z-score in the `value` field and sets `z_score=0`; downstream `s.z_score * s.weight` at `oracle/engine.py:1371` then contributes zero, neutralizing registry-sourced signals whenever `GRID_SIGNAL_REGISTRY=1` is enabled. **RESOLVED 2026-05-30 (commit 8de99c4e):** call at `oracle/engine.py:835` now passes `Signal(name, family, z, z, sig_dir, conf, 0)` so `z_score` carries the registry z; regression test at `tests/test_oracle_registry_signals.py`.
- [ ] [P2] Split `oracle/engine.py` (2,793 LOC) into focused modules — `oracle/engine.py:1` — file is well past the 1,500-LOC threshold; obvious carve-outs are the `EnsemblePrediction` + `EnsemblePredictor` block (line 2229+) into `oracle/ensemble.py` and the second copy of horizon helpers (line 2158+) into a shared `oracle/horizons.py`.
- [ ] [P2] Refactor `OracleEngine.predict` (405 lines) into smaller passes — `oracle/engine.py:2274` — one of three over-100-line methods in the file; current shape blends regime fetch, FCI compute, shapley, crowdedness, market-implied prob, and journal-bias adjustment in one function.
- [ ] [P2] Refactor `OracleEngine._oracle_one_ticker` (299 lines) — `oracle/engine.py:1189` — per-ticker signal gather + anti-signal scan + convergence boost + confidence normalization in a single function; extract `_apply_credit_family_boost`, `_apply_actor_signal_enrichment`, `_apply_convergence_boost` helpers.
- [x] [P2] Add unit tests for `oracle/citation_extractor.py` feature-mention matching — `oracle/citation_extractor.py:50` — `extract_citations` is called from `api/routers/chat.py:1450` to record which features the LLM cited; covered behavior is alias/family normalization, no tests exist. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_citation_extractor.py` (218 LOC, 28 test cases) covers `extract_citations`.
- [x] [P2] Add unit tests for `oracle/psi_model.py` PSI+VIX gating thresholds — `oracle/psi_model.py:137` — `evaluate_psi_signals` is the entry point used by `scripts/run_psi_oracle.py:26`; the hardcoded Sharpe-2.59 GLD config and Sharpe-2.01 QQQ config thresholds have no regression coverage. **RESOLVED 2026-05-30 (commit 8de99c4e):** `tests/test_psi_model.py` (385 LOC, 25 test cases) covers `evaluate_psi_signals`.

## Auditor 2026-05-17 — api/

Metrics: 107 py files, 48,162 total LOC, 4 commits in last 30d.
Checks run: A B C D E F G H I J

> **2026-06-16 reconciliation note:** the listed API routine PRs have merged,
> so the resolved pagination, `system.py` coverage, `prediction_backtest`
> engine-import, `clear_singletons()`, and `viz` smoke-test items are now marked
> `[x]` below with PR/commit evidence.

- [x] [P1] Add `has_more` to `search_intelligence` paginated response — `api/routers/intelligence_search.py:131` — endpoint takes `limit`/`offset` Query params but the return shape is `{results, total, query}` only; `.claude/rules/security.md` requires list endpoints to return `total` + `limit` + `offset` + `has_more` so clients can paginate without re-computing. **RESOLVED 2026-06-16 (PR #239, commit 28d340d5):** added `limit`/`offset`/`has_more` to empty, error, and normal responses.
- [x] [P1] Add `has_more` to `oracle.get_predictions` response — `api/routers/oracle.py:168` — returns `total/limit/offset` but not `has_more`; same security-rule pagination contract as the journal endpoint at `api/routers/journal.py:78` which is the canonical pattern to copy. **RESOLVED 2026-06-16 (PR #240, commit 7a353de9):** response now includes `has_more`.
- [x] [P1] Add `has_more` to `models.get_all` response — `api/routers/models.py:67` — same `total/limit/offset` without `has_more` gap; this is the model-registry list, often paged by the PWA model browser. **RESOLVED 2026-06-16 (PR #255, commit e96d5e4c):** response now includes `has_more`.
- [x] [P1] Add pagination metadata to `intel.intel_search` — `api/routers/intel.py:252` — accepts `limit`/`offset` but the `_ok(...)` envelope returns no `total` / `limit` / `offset` / `has_more`; client gets a results array with no signal that more pages exist. **RESOLVED 2026-06-16 (PR #297, commit 10f62d98):** added pagination metadata.
- [x] [P1] Add `has_more` to `intel.intel_predictions_active` meta — `api/routers/intel.py:1384` — currently emits `meta={"total": total, "limit": limit, "offset": offset}` but no `has_more`; small uniformity fix on a hot intel endpoint. **RESOLVED 2026-06-16 (PR #256, commit f8240d04):** meta now includes `has_more`.
- [x] [P1] Add direct test coverage for `api/routers/system.py` — `api/routers/system.py:64` — 1,686-LOC router holds `/health`, `/status`, `/freshness`, `/pipeline-health`, `/hermes-status`, `/services`; only `/health` and `/status` are touched by `tests/test_api.py:47`; create `tests/test_system_router.py` exercising at least `/freshness`, `/pipeline-health`, `/hermes-status` happy paths. **RESOLVED 2026-06-16 (PR #265, commit ad1d1950):** direct router tests added.
- [x] [P1] Stop importing `get_engine` from `api.dependencies` in `prediction_backtest.py` — `api/routers/prediction_backtest.py:20` — re-export only works because the name is in module scope; the other 9 routers use `get_db_engine` (the clearable wrapper). Switch to `get_db_engine` for consistency with the singleton-clearing contract documented in `.claude/rules/security.md`. **RESOLVED 2026-06-16 (PR #275, commit b948d829):** import now uses `get_db_engine`.
- [x] [P2] Fix `clear_singletons()` so it actually replaces the engine — `api/dependencies.py:67` — function disposes `_db_engine` and sets api-level pointer to None, but the underlying `db._engine` singleton at `db.py:46` is not cleared, so the next `get_db_engine()` returns a *disposed* engine. Add `import db; db._engine = None` (or expose a `db.clear_engine()` helper) so the contract advertised in the docstring holds. **RESOLVED 2026-06-16 (PR #277, commit 8aa2001e):** reset now clears the underlying engine singleton.
- [ ] [P2] Replace f-string SQL in `prediction_backtest.dataset_stats` with parameterized table-validated query — `api/routers/prediction_backtest.py:116` — `text(f"SELECT COUNT(*) FROM {table}")` interpolates a table name into SQL; today the values come from a hardcoded list literal so it's safe, but the pattern violates `.claude/rules/security.md` ("NEVER use f-strings ... for SQL queries"). Switch to explicit per-table queries or an allowlist+validate helper like the one at `api/routers/config.py:27`.
- [ ] [P2] Split `canvas_expand.expand_node` (737 LOC) into stage helpers — `api/routers/canvas_expand.py:219` — single function blends actor lookup, signal fetch, evidence rollup, layout, and serialization; extract `_collect_neighbor_actors`, `_attach_signals`, `_attach_evidence`, `_build_layout` as separate functions in the same file (no new modules).
- [ ] [P2] Split `intel.intel_briefing` (509 LOC) into per-section helpers — `api/routers/intel.py:1650` — function returns a multi-section briefing payload; each section (macro, sectors, tickers, news, etc.) is an obvious extraction target with no shared mutable state.
- [ ] [P2] Split `intelligence_risk._build_risk_map` (448 LOC) — `api/routers/intelligence_risk.py:43` — pre-DI bug magnet; pull per-region risk computation and the final assembly step into named helpers so the route handler stays under 100 LOC.
- [ ] [P2] Split `flows._build_sector_connections` (464 LOC) — `api/routers/flows.py:877` — sector graph construction; extract `_score_edges`, `_normalize_weights`, `_label_communities` helpers to bring the body under the 100-LOC review threshold from `.claude/rules/common/coding-style.md`.
- [x] [P2] Add a smoke test for the 9 unauthenticated `viz` routes — `api/routers/viz.py:28` — `/api/v1/viz/recommend`, `/rules`, `/weights`, `/spec/*` are mounted without `require_auth`. They return canned VizSpec payloads (no DB writes, no PII), but the no-auth surface should be locked in with a test so a future edit can't accidentally widen exposure (a regression check at minimum: GET `/api/v1/viz/rules` returns 200 without a token). **RESOLVED 2026-06-16 (PR #292, commit af646e56):** unauthenticated route smoke tests added.

## Auditor 2026-05-24 — intelligence/

Metrics: 173 py files, 103,106 total LOC, 22 commits in last 30d.
Checks run: A B C D E F G H I J

- [ ] [P1] Add test coverage for `intelligence/global_levers.py` — `intelligence/global_levers.py:1584` — 2,254-LOC macro-lever module with zero test references anywhere; its public API (`get_lever_hierarchy`, `get_lever_domain`, `trace_lever_chain`, `find_cross_domain_actors`, `generate_lever_report`) is served live via `api/routers/intelligence_deepdive.py` with no regression guard. Create `tests/test_global_levers.py` covering at least `get_lever_hierarchy` and `trace_lever_chain` happy paths.
- [ ] [P1] Add test coverage for `intelligence/deep_graph.py` — `intelligence/deep_graph.py:940` — 1,771-LOC multi-hop graph traversal engine consumed by `intelligence/actor_network.py`, `intelligence/power_mapper.py`, and `api/routers/intelligence_companies.py`, yet has zero test references. `deep_drill`, `find_overlaps`, and `find_all_overlaps` are the public entry points; add `tests/test_deep_graph.py` exercising `find_overlaps` on a small fixture graph.
- [x] [P1] Add test for signal-weight override resolution — `intelligence/signal_weight_overrides.py:135` — `get_override()` multiplies signal weights on the LIVE conviction path (called by both `oracle/engine.py` and `intelligence/signal_provenance.py`) but the 154-LOC module has no test file and zero test refs; an override bug silently mis-weights every prediction. Test `get_override` for known/unknown signals and the `set_enabled` toggle. **RESOLVED 2026-06-16 (PR #317, commit 8b48fc59):** added focused tests for known/unknown override resolution and the enabled toggle.
- [x] [P1] Add test for `assess_pocket_lining` self-dealing detector — `intelligence/pocket_lining.py:24` — logic-bearing detector (politician-committee overlap, fund-vs-personal divergence, insider-before-news) served via `api/routers/intelligence_actors.py` with no test file; false positives/negatives here are user-facing accusations. Add `tests/test_pocket_lining.py` with fixtures for at least the committee-overlap branch. **RESOLVED 2026-06-16 (PR #319, commit 7fa77951):** added committee-jurisdiction overlap coverage for `assess_pocket_lining`.
- [x] [P1] Add test for `intelligence/scheduler.py` — `intelligence/scheduler.py:20` — the module docstring states it was "extracted from api/main.py lifespan ... to make the scheduler independently testable," but no test exists; it drives all periodic intel tasks (briefings, flow refresh, options tracking, paper trading). Add `tests/test_scheduler.py` that asserts the expected jobs get registered (mock `schedule`), no live loop needed. **RESOLVED 2026-06-16 (PR #318, commit 783f4ab3):** added scheduler registration coverage without running live scheduled jobs.
- [ ] [P2] Split `run_intelligence_loop` (930 LOC) — `intelligence/scheduler.py:20` — single function registers and runs every periodic job inline; extract per-domain registration helpers (`_register_briefings`, `_register_flows`, `_register_options`, etc.) so the loop body drops under the 100-LOC review threshold and the registration logic becomes unit-testable.
- [ ] [P2] Split `build_provenance_report` (316 LOC) — `intelligence/signal_provenance.py:513` — this is the core 13-layer conviction-stack assembler; pulling each adjuster-fetch block into a named helper would let the layers be tested in isolation and keep the orchestrator readable (no new module, same file).
- [ ] [P2] Split `_search_upstream_by_type` (341 LOC) — `intelligence/causation_graph.py:561` — single recursive search function blends query construction, scoring, and result assembly; extract the per-type query builders so the traversal core stays under 100 LOC.
- [ ] [P2] Split `import_icij_offshore` (318 LOC) — `intelligence/actor_discovery.py:2721` — ICIJ import mixes CSV parsing, entity resolution, and DB upsert in one body; extract `_parse_icij_row`, `_resolve_icij_entity`, `_upsert_offshore_link` to make the import path testable with a small fixture file.
- [ ] [P2] Split `_fetch_live_lever_data` (277 LOC) — `intelligence/global_levers.py:1720` — one function fetches every live lever source sequentially; extract per-source fetch helpers so a single failing source can be tested/mocked in isolation without driving the whole fetch.

## Auditor 2026-05-31 — contracts/

Metrics: 23 py files, 2,663 total LOC, 1 commit in last 30d.
Checks run: A B C D E F G H I J

- [x] [P0] Fix schema mismatch in `pull_lifecycle.on_pull_lifecycle` — `contracts/handlers/pull_lifecycle.py:21` — handler reads `evt.status` and `evt.rows`, but `PullLifecycle` schema (`contracts/schemas.py:163-168`) defines fields `state` and `row_count`. `BaseContract` is `extra="forbid"` + frozen, so `getattr(evt, "status", "")` always returns `""` — the FAILED branch never fires and the operator never sees puller-failure warnings via this handler. Rename to `state`/`row_count` and update the FAILED check. **RESOLVED 2026-06-16 (PR #311, commit 52aaf428):** handler/schema field mismatch fixed.
- [x] [P1] Fix wrong `source_type` in `trust.on_edge_validated` — `contracts/handlers/trust.py:208` — UPDATE filters `s.source_type = 'cross_lens'`, but the production source_type used by `oracle/engine.py:914` and `intelligence/trust_scorer.py:59,81,97` is `'cross_lens_supply_shock'` (cf. companion `contracts/handlers/edges.py:65` which gets it right). The handler runs, logs `cross_lens_sources_downgraded=0`, and the §7.3 source-trust decay loop is silently dead. **RESOLVED 2026-06-16 (PR #313, commit 63458182):** handler now targets `cross_lens_supply_shock` and regression coverage proves legacy `cross_lens` rows are not decayed.
- [x] [P1] Enforce documented handler ordering on `PredictionScored` — `contracts/dispatcher.py:106` — router comment at `contracts/router.py:42-43` declares "trust must update before oracle_weights so the weight delta sees the fresh source posteriors (§8.2 ordering invariant)", but `_on_event` submits all 4 handlers to an 8-worker `ThreadPoolExecutor` in parallel with no ordering guarantee. Either run the route serially (per-contract sequential, cross-contract parallel) or document that ordering is best-effort and revise §8.2. **RESOLVED 2026-06-16 (PR #315, commit d34cfa03):** one emitted contract route now runs handlers serially in `ROUTES` order while preserving cross-contract pool concurrency.
- [x] [P1] Add unit tests for `alerts.on_cross_reference_anomaly` and `alerts.on_regime_transition` — `contracts/handlers/alerts.py:71,121` — paging path (HIGH/CRITICAL severity, dedup via `contract_alert_log`, late-imported alerters) has zero coverage in `tests/test_synth_wave_a.py`/`_b.py`/`_c.py`; a regression that breaks paging will not be caught. **RESOLVED (docs reconciliation 2026-07-01):** `tests/contracts/test_alerts_handler.py` (195 LOC, 8 tests) covers both handlers directly — `on_cross_reference_anomaly` MEDIUM/HIGH/CRITICAL severity gating plus `on_regime_transition` payload assembly.
- [x] [P1] Add unit tests for `oracle_anti_signals.on_cross_reference_anomaly` — `contracts/handlers/oracle_anti_signals.py:36` — severity gating + table auto-create + idempotent insert path has no direct test; the §7.3 CrossReferenceAnomaly route fully depends on this handler. **RESOLVED 2026-06-25 (PR #344, commit 3a7a504b):** `tests/contracts/test_oracle_anti_signals_handler.py` (258 LOC, 8 tests) covers severity gating (LOW skip / MEDIUM+ persist / unknown skip), CREATE TABLE IF NOT EXISTS shape, `ON CONFLICT (event_id) DO NOTHING` idempotency, insert-param payload shape, JSON-encoding of `evidence_links`, and the non-fatal exception swallow contract.
- [x] [P1] Add unit tests for `oracle_regime.on_regime_transition` — `contracts/handlers/oracle_regime.py:27` — auto-creates `regime_transitions_audit` and writes on every emission; no test covers the JSON payload shaping or `ON CONFLICT DO NOTHING` idempotency claim. **RESOLVED 2026-06-16 (PR #320, commit 2ed241ed):** added fake-engine tests for JSON payload params and idempotent insert SQL.
- [ ] [P1] Add unit tests for `trust.on_signal_fired` and `trust.on_edge_validated` — `contracts/handlers/trust.py:97,176` — direction-inference branches (signal_type → BUY/SELL fallback to sign-of-strength) and the (broken) cross_lens source decay both lack direct tests; would catch the source_type bug above and the strength=0 skip path.
- [ ] [P1] Add a unit test for `journal.on_prediction_scored` — `contracts/handlers/journal.py:168` — closes out provisional `decision_journal` rows by joining `source_contract_id` against `contracts_audit.correlation_id`; verdict→outcome mapping and "no row to close" paths are untested.
- [x] [P1] Add a unit test for `calibration.on_options_trade_outcome` — `contracts/handlers/calibration.py:61` — synthetic `model_id=f"strategy:{strategy}"` write to `update_running_metrics` is untested; companion `on_prediction_scored` is covered in `tests/test_synth_wave_a.py:146` but this one slipped. **RESOLVED 2026-06-16 (PR #321, commit 53f78453):** added positive/non-positive PnL mapping tests and missing-strategy no-op coverage.
- [x] [P2] Preserve Decimal precision in `oracle_anti_signals.on_cross_reference_anomaly` — `contracts/handlers/oracle_anti_signals.py:127` — `official_value` and `reality_proxy_value` are `Decimal` on the schema and `NUMERIC` in the DB, but the handler coerces them through `float(...)` before binding, silently truncating precision for cross-reference deltas. Pass the Decimal through directly (SQLAlchemy will bind it to NUMERIC) or use `:ov` with `str(decimal)` and `CAST(:ov AS NUMERIC)`. **RESOLVED 2026-07-02 (commit 95fdd4de):** handler now serializes both fields via `_decimal_bind()` (`str(Decimal)`) and binds through `CAST(:ov AS NUMERIC)` / `CAST(:rv AS NUMERIC)`, sidestepping DBAPI float coercion. Regression coverage in `tests/contracts/test_oracle_anti_signals_handler.py::test_decimal_values_preserved_as_string_for_numeric_cast` (a 34-digit `Decimal` round-trips losslessly; the `float()` path is asserted to lose precision).
- [x] [P2] Update stale docstring on `contracts/handlers/__init__.py` — `contracts/handlers/__init__.py:1` — says `"Phase 2 contract handlers — empty in Phase 1."`, but the directory now ships 11 handler modules totalling 1,420 LOC and `router.py` advertises Phase 2 as fully populated. One-line docstring update. **RESOLVED 2026-07-10:** rewrote the docstring to list the 11 shipped handler modules and point at `router.py::ROUTES`; regression test at `tests/contracts/test_public_api.py::test_handlers_docstring_reflects_shipped_modules`.
- [ ] [P2] Drop dead defensive branch in `oracle_anti_signals` severity check — `contracts/handlers/oracle_anti_signals.py:60-75` — first `if severity not in _MIN_PERSIST_SEVERITY and severity != "LOW"` can never fire because Pydantic `Literal["LOW", "MEDIUM", "HIGH", "CRITICAL"]` on the schema rejects anything else at construction (`contracts/schemas.py:115`). Collapse to a single check for the LOW skip.
- [ ] [P2] Add type hints to `Dispatcher.__init__` — `contracts/dispatcher.py:30-36` — `bus` and `engine` parameters are untyped. The handler call signature is documented as `(contract, *, engine: Engine)`, so `engine` can be `sqlalchemy.engine.Engine`; `bus` matches the `events.bus.Bus` interface. Tightens the public surface and helps mypy/IDE.
- [ ] [P2] Decide on the unused `pull_lifecycle` context manager — `contracts/emit.py:118` — the manager is exported from `contracts/__init__.py:13,17` but no ingestion module in `ingestion/` or `scripts/` calls it (only tests import it). Either wire it into the puller base or remove from the public surface so it's not adopted with the broken consumer at `contracts/handlers/pull_lifecycle.py:21`.

## Auditor 2026-06-07 — alpha_research/

Metrics: 21 py files, 3,592 total LOC, 1 commit in last 30d.
Checks run: A B C D E F G H I J

- [x] [P0] Replace synthetic random VIX history in `run_rotation` with real DB query — `alpha_research/strategies/adaptive_rotation.py:321` — when only the current VIX value is available, the function fabricates a 20-day history via `np.random.normal(vix_ma, vix_ma * 0.15, 20)` and feeds it to `detect_regime` for z-score computation. No seed is set, so every call to the live rotation strategy gets a *different* `vix_zscore` and can flip between `risk-on` / `neutral` / `risk-off`. `run_rotation` is invoked from `scripts/live_rotation_trader.py`, `scripts/rotation_paper_trader.py`, and `hermes_operator.py`. Pull the real 20-day VIX series via `get_vix_series(engine, start=as_of-30d, end=as_of)` or `compute_vix_exposure_series` — both already exist in this directory. **RESOLVED 2026-06-16 (PR #302, commit bc51829c):** synthetic VIX history path removed in favor of real history.
- [ ] [P1] Add `as_of_date` parameter and `release_date <= :as_of` clauses to `conviction_scorer.py` helpers — `alpha_research/conviction_scorer.py:54` — `_load_latest`, `_load_raw_latest`, `_load_raw_series`, `_load_price` all query `resolved_series` / `raw_series` with `ORDER BY obs_date DESC LIMIT 1` and no PIT guard. `score_ticker` / `scan_all` are exposed live via `api/routers/signals.py:194,226`; CLAUDE.md mandates that all inference paths go through PIT-correct queries (see `alpha_research/data/panel_builder.py:67` for the canonical `release_date <= :as_of` pattern). Today the omission only hides PIT bugs because there's no `as_of` plumbing; adding the parameter is the prerequisite to using this scorer in any backtest.
- [x] [P1] Seed `np.random` in `permutation_test` and `subsample_stability` for reproducible gauntlet verdicts — `alpha_research/validation/gauntlet.py:64` and `alpha_research/validation/gauntlet.py:136` — both use `np.random.permutation` / `np.random.shuffle` without a seed, so the same signal+returns input produces different `permutation_p` and `subsample_stability` values across runs, which can flip the `ROBUST` / `MARGINAL` / `UNSTABLE` verdict at line 257. Thread a `seed: int = 42` arg through `run_gauntlet` → both subroutines and switch to `np.random.default_rng(seed)`. **RESOLVED 2026-06-16 (PR #303, commit ed26fe89):** deterministic seed added.
- [x] [P1] Add unit tests for `conviction_scorer.py` 7 layer scorers + main entry — `alpha_research/conviction_scorer.py:113` — zero tests cover `score_setup`, `score_company`, `score_smart_money`, `score_crowd`, `score_narrative`, `score_flow`, `score_confirmation`, `score_ticker`, `scan_all`, despite live exposure at `api/routers/signals.py:194`. Mock `conn.execute(...).fetchone/fetchall` returns to verify score bucketing thresholds and the CONVICTION/FIRE/SCALE/PILOT/WATCH/PASS level mapping at line 440. **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_conviction_scorer.py` (428 LOC, 15 tests) covers `score_setup`, `score_company`, `score_ticker` bucketing + level mapping plus PIT `as_of_date` plumbing.
- [x] [P1] Add unit tests for `signal_adapter.py` publish functions — `alpha_research/adapters/signal_adapter.py:44` — `publish_factor_signals`, `publish_regime_signal`, `publish_all_alpha_signals` are called by `scripts/hermes_operator.py:1972` on every signal-publish cycle. Untested behaviour includes the contrarian-vs-momentum direction inversion at line 91-106 (the bug-fix postmortem documented at line 26-30 prevents another regression like the dual_horizon 579-wrong miss). **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_signal_adapter.py` (416 LOC, 6 tests) covers `publish_factor_signals` direction inversion, registry payload shape, and sparse-panel skip path.
- [x] [P1] Add unit tests for `rotation_variant_backtest.backtest_rotation_variant` — `alpha_research/strategies/rotation_variant_backtest.py:101` — function writes to `astrogrid.backtest_run` and `astrogrid.backtest_result` (line 269/292) with no test coverage; one happy-path test with a small synthetic price panel and mocked `engine.begin()` would lock the metric-payload schema down so a future change can't silently corrupt the backtest table. **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_rotation_variant_backtest.py` (184 LOC, 1 happy-path test) locks the persisted summary payload shape with a synthetic price panel and MagicMock engine.
- [x] [P1] Add unit tests for `compute_credit_cycle` and `compute_vix_exposure_scalar` — `alpha_research/signals/credit_cycle.py:30` and `alpha_research/signals/exposure_scaler.py:25` — both are published to the SignalRegistry every cycle through `publish_all_alpha_signals`; together they drive the `oracle/engine.py::_get_credit_cycle_routing` family-weight multipliers. Test the contraction/expansion + calm/elevated/stressed bucketing thresholds with deterministic series. **RESOLVED 2026-06-16 (PR #306, commit 039bf6f9):** deterministic scalar tests added.
- [ ] [P1] Add unit tests for `split_adjuster.py` — `alpha_research/data/split_adjuster.py:36` — `detect_splits`, `adjust_splits`, `adjust_panel`, `compute_real_drawdown` form the universal split-adjustment layer that every price panel flows through (`panel_builder.build_price_panel:89`); zero tests despite the file's docstring claiming "every downstream consumer gets clean data." Cover the multi-split compounding case (e.g., TSLA 5:1 then 3:1) called out in the `adjust_splits` docstring.
- [ ] [P2] Delete dead module `alpha_research/data/shares_tracker.py` (183 LOC) — `alpha_research/data/shares_tracker.py:1` — verified zero callers outside the module via grep across the repo (only docstring mentions in `docs/decisions/ML-Inference.md` and the inventory). All 5 public functions (`fetch_daily_fundamentals`, `compute_shares_outstanding`, `detect_dilution_events`, `market_cap_adjusted_return`, `get_dilution_adjusted_price`) are unreferenced. Appears unused, verify before removing.
- [ ] [P2] Delete dead helpers `compute_vix_exposure_series` and `build_returns_panel` — `alpha_research/signals/exposure_scaler.py:101` and `alpha_research/data/panel_builder.py:151` — both functions have zero callers anywhere in the repo; only the inventory docs reference them. Appears unused, verify before removing.
- [ ] [P2] Log instead of silently swallowing in heartbeat exception handlers — `alpha_research/heartbeat.py:111` and `alpha_research/heartbeat.py:137` — both `_check_puller_health` and `_check_pit_freshness` do `except Exception: pass`, which means the heartbeat reports "all clear" even when its own checks crashed. Per `CLAUDE.md` log-level guidance, convert to `log.warning("heartbeat check failed", err=exc)` so failures surface in `errors.jsonl` without crashing the cycle.
- [x] [P2] Log skipped rebalances in `backtest_rotation_variant` — `alpha_research/strategies/rotation_variant_backtest.py:153` — the `except Exception as exc:` block binds `exc` and immediately `continue`s without logging; the leading comment ("Skip this rebalance, log to outcome") promises a log that never happens. Adding `log.warning("rebalance failed", asof=asof, err=str(exc))` makes silent dropped rebalances visible in backtest postmortems. **RESOLVED 2026-06-30:** added `from loguru import logger as log` import and replaced the silent `continue` with `log.warning("rotation_variant_backtest: run_rotation failed asof={asof}: {err}", ...)`. Regression coverage in `tests/test_rotation_variant_backtest_logging.py` (2 cases: failing + clean run).
- [ ] [P2] Remove or restore the stale `docs/TODO-REGIME-SIGNAL-USAGE.md` reference — `alpha_research/adapters/signal_adapter.py:174` — comment points readers at a follow-up doc that does not exist in the repo (`ls docs/TODO-REGIME-SIGNAL-USAGE.md` → no file). Either rewrite the inline note with the actual follow-up plan or drop the dangling reference.
- [ ] [P2] Drop the unnecessary `object.__setattr__` workaround on `PositionState` — `alpha_research/strategies/adaptive_rotation.py:95` — class is declared `@dataclass` (mutable, not `frozen=True`), so `update_peak` can simply assign `self.peak_price = price` / `self.peak_date = dt`. The `object.__setattr__` calls look like a leftover from a `frozen=True` revision and confuse readers into thinking the dataclass is immutable.

## Auditor 2026-06-14 — physics/

Metrics: 18 py files, 4,824 total LOC, 1 commit in last 30d.
Checks run: A B C D E F G H I J

- [ ] [P1] Batch `_get_latest_value` calls in `check_dimensional_consistency` to one query — `physics/verify.py:354` — the loop iterates all `model_eligible` features (~1,188 per CLAUDE.md) and calls `self._get_latest_value(name, ...)` up to twice per row (line 360/370 then line 379), and `_get_latest_value` opens a fresh `self.engine.connect()` + JOINed SELECT each call; this is a textbook N+1 (~2.4k DB roundtrips per `verify_all`). Replace with a single CTE that returns name→latest_value for all eligible features in one round-trip, then map in Python.
- [x] [P1] Resolve `transfer_entropy` drift — keep one canonical implementation — `physics/transforms.py:627` vs `analysis/transfer_entropy.py:135` — same function name in two trees with divergent bodies (physics version: 4-term entropy decomposition; analysis version: lagged-MI binning with extra helpers); the analysis version is the only one with callers (`analysis/lead_lag_backtest.py` + `transfer_entropy.py:206-207`), and the physics one has zero callers. Delete `physics/transforms.py:627` or re-export from `analysis.transfer_entropy` so a future import doesn't accidentally pick the wrong body. **RESOLVED 2026-06-19:** deleted the `physics/transforms.py:627` body (zero callers, verified via repo-wide grep); left a comment pointing future imports at `analysis.transfer_entropy`. Regression guard in `tests/test_transfer_entropy_canonical.py` asserts that if `physics.transforms` ever re-exposes `transfer_entropy` it must be the same callable object as `analysis.transfer_entropy.transfer_entropy`.
- [x] [P1] Add unit tests for `physics/transforms.py` energy + OU helpers — `physics/transforms.py:32` — 679 LOC and 17 public functions (kinetic/potential/total/market_temperature/entropy_rate/phase_velocity/estimate_ou_parameters/...) with zero direct tests in `tests/`; the live wiring at `features/lab.py:374,382,390,402,412` and `api/routers/physics.py:140,172,210,293` means a regression here silently corrupts feature outputs. Start with reference-value tests for the 6 functions actually called in prod (kinetic_energy, potential_energy, total_energy, market_temperature, estimate_ou_parameters, hurst_exponent). **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_physics_transforms.py` (136 LOC, 9 tests) covers the six prod-called helpers with reference values (kinetic_energy, potential_energy, market_temperature, estimate_ou_parameters, hurst_exponent).
- [x] [P1] Add unit tests for `physics/conventions.py` validators — `physics/conventions.py:197` — `validate_convention` is called from `physics/verify.py:381` on every model-eligible feature during dimensional checks; `validate_feature_set` and `check_unit_compatibility` are public API. Zero tests cover the range checks, the rates/spreads/volatility/returns heuristics, or the spread/ratio unit-compatibility branches. **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_physics_conventions.py` (181 LOC, 13 tests) covers `validate_convention`, `validate_feature_set`, and `check_unit_compatibility` across rate/spread/volatility/return heuristics.
- [x] [P1] Add unit tests for `physics/waves.py:build_execution_waves` — `physics/waves.py:41` — used by `cli.py:312` and `api/routers/workflows.py:124` to topo-sort GRID workflow tasks; zero tests cover the dependency cycle detection, isolated-task wave assignment, or the `WaveTask` field defaults. **RESOLVED 2026-06-16 (PR #314, commit 566af778):** direct wave-builder tests cover `WaveTask` defaults, dependency ordering, isolated first-wave tasks, and cycle errors.
- [x] [P1] Add direct unit tests for `DealerGammaEngine.compute_gex_profile` arithmetic — `physics/dealer_gamma.py:118` — the engine is exercised today only via mocks in `tests/test_options_scanner_dealer_extras.py` (which `.return_value = profile` an entire pre-computed dict, never running the real math); the per-strike GEX aggregation, `_find_gamma_flip` interpolation, gamma/put/call wall selection, and regime tagging have no direct test coverage despite feeding `ollama/dealer_flow_briefing.py:117`, `valuation/derivatives_support.py:504`, and `discovery/options_scanner.py`. **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_dealer_gamma.py` (205 LOC, 3 tests) exercises `compute_gex_profile` per-strike aggregation, `_find_gamma_flip` interpolation, and normalized-GEX regime tagging directly (no mocked return value).
- [x] [P1] Add direct tests for `check_dimensional_consistency`, `check_regime_boundaries`, `check_stationarity` — `physics/verify.py:322` — `tests/test_physics.py` only exercises the no-data paths of `check_limiting_cases`/`check_numerical_stability`/`check_conservation` and the integration of `check_news_momentum`; the three non-trivial check methods above (each 94-126 LOC of logic) are uncovered, and `verify_all` swallows their exceptions at line 96-108 so silent regressions wouldn't surface in CI. **RESOLVED (docs reconciliation 2026-07-01):** `tests/test_physics_verify.py` (274 LOC, 6 tests) covers `check_dimensional_consistency`, `check_regime_boundaries`, and `check_stationarity` directly.
- [ ] [P2] Delete 9 unused public functions from `physics/transforms.py` (appears unused, verify before removing) — `physics/transforms.py:121` — `entropy_rate` (121), `phase_velocity` (155), `ou_mean_reversion_signal` (261), `ou_displacement` (290), `langevin_drift` (322), `langevin_diffusion` (372), `fokker_planck_density` (421), `relaxation_time` (484), `rolling_hurst` (600) have zero callers across the repo (excluding tests / self); each is 30-60 LOC of stochastic-process math that hasn't found a consumer. Confirm with one final repo-wide grep (string-based imports could hide a caller) before removing.
- [ ] [P2] Delete `execute_waves` and `build_grid_pipeline_waves` from `physics/waves.py` (appears unused, verify before removing) — `physics/waves.py:107` and `physics/waves.py:227` — both are publicly exported but have zero callers anywhere in the repo; the wave-execution callers (`cli.py:312`, `api/routers/workflows.py:124`) use only `WaveTask` + `build_execution_waves` and implement their own execution loop. Removing trims ~145 LOC and the unused `concurrent.futures` import.
- [ ] [P2] Split `NewsEnergyEngine.analyze` (185 LOC) into per-stage helpers — `physics/news_energy.py:320` — single method blends feature discovery, raw fetch, per-source KE/PE computation, coherence/force-vector aggregation, regime-signal detection, and summary build; extract `_compute_per_source_energy`, `_compute_coherence`, `_build_force_vector`, `_detect_regime_signal` so the orchestrator stays under 100 LOC.
- [ ] [P2] Split `MarketPhysicsVerifier.check_regime_boundaries` (126 LOC) into transition checks — `physics/verify.py:404` — the function rolls the no-instant-jump rule, persistence threshold, and transition-frequency stats into one body; extract `_validate_transition_path`, `_check_persistence`, `_summarize_transitions` for testability of each rule in isolation.

## Auditor 2026-06-21 — analysis/

Metrics: 33 py files, 18,973 total LOC, 1 commit in last 30d (abandonment-risk territory — `7a353de feat(api): add has_more …` is a stray API-router change; the actual analysis modules have not seen a commit in the 90-day window).
Checks run: A B C D E F G H I J

- [ ] [P1] Add unit tests for `analysis/thesis_scorer.py` — `analysis/thesis_scorer.py:65` — 2,745 LOC across 28 sub-scorers (`_score_fed_liquidity` / `_score_dealer_gamma` / `_score_vanna_charm` / `_score_congressional` / …) with zero direct test references; `score_thesis` and `snapshot_thesis` are called by `api/routers/intelligence_thesis.py:50` (live chat brief), `api/routers/chat.py:751` (`_score_geopolitical_risk`), and `scripts/hermes_operator.py:637` (every Hermes thesis-snapshot cycle), so a regression here silently corrupts the public thesis card. Start with the 4 sub-scorers most-likely to be re-touched (`_score_fed_liquidity`, `_score_dealer_gamma`, `_score_congressional`, `_score_news_sentiment`) using `MagicMock` for the `Engine` to assert verdict-banding math.
- [ ] [P1] Add unit tests for `analysis/money_flow.py` build/drill helpers — `analysis/money_flow.py:1412` — 1,770-LOC module wired to prod via `api/routers/flows.py:1841` (`build_flow_map`) and `:1863` (`get_company_drill`); zero direct tests. The drill paths assemble layered narratives + intelligence overlays and any silent break shows up as a blank Flow Map card in the PWA. Test `build_flow_map` happy path with a `MagicMock` engine + a small dictionary of stubbed layer rows; test `get_company_drill` for a known ticker shape.
- [ ] [P1] Add unit tests for the `analysis/money_flow_engine/` 8 layer builders — `analysis/money_flow_engine/__init__.py:31` — package's `build_flow_map` is consumed by `intelligence/money_flow_adapter.py:38` (the 14th conviction-stack adjuster), `intelligence/deep_dive.py:196`, `intelligence/audio_briefing.py:146`, `intelligence/image_gen.py:184`, and `api/routers/flows.py:1946,1956,2037,2053`; only `analysis/money_flow_engine/types.py` (dataclasses) is touched by `tests/test_money_flow_adapter.py`. Cover `build_monetary_layer` (highest-traffic) and `build_credit_layer` (where the silent-except below lives) end-to-end with a stubbed engine to lock node-count + confidence-tag invariants.
- [ ] [P1] Add unit tests for `analysis/flow_thesis_data.py` highest-traffic state-getters — `analysis/flow_thesis_data.py:431` — 1,413-LOC module exposing 24 `_get_*_state` functions; routed through `analysis/flow_thesis.py` to `api/routers/intelligence_thesis.py:50`, `intelligence/audio_briefing.py`, `intelligence/market_diary.py`, `intelligence/thesis_tracker.py`. Pick the 4 highest-traffic theses (`_get_fed_liquidity_state`, `_get_dealer_gamma_state`, `_get_institutional_rotation_state`, `_get_cross_reference_state`) and lock the state-payload shape so a future schema drift fails in CI instead of in the briefing.
- [ ] [P1] Add unit tests for `analysis/capital_flows.py:CapitalFlowResearchEngine` — `analysis/capital_flows.py:77` — 892-LOC research engine with zero tests; live consumers are `api/routers/ollama.py:320` and `intelligence/scheduler.py:44`. The class has 30+ methods doing per-actor flow rollups; start with the constructor + one happy-path rollup using a `MagicMock` engine to anchor return-shape invariants.
- [ ] [P1] Add unit tests for `analysis/vol_surface.py:VolSurfaceEngine` SVI fit + Black-Scholes greeks — `analysis/vol_surface.py:113` — 1,271-LOC engine called from `discovery/options_scanner.py:503` to build a fresh SVI surface per expiry, then fed into the dealer-gamma + valuation paths. Today's only coverage (`tests/test_options_scanner_vol_surface.py`) mocks the engine entirely. Cover the six private `_bs_*` helpers (`_bs_vega`, `_bs_theta_call`, `_bs_theta_put`, `_bs_volga`, `_bs_call_price`, `_bs_put_price`) against textbook Black-Scholes values — 6 tests, ~40 LOC — before tackling the SVI fit.
- [x] [P1] Add unit tests for `analysis/ephemeris.py:Ephemeris` core solvers — `analysis/ephemeris.py:294` — 1,034-LOC Kepler/ecliptic solver feeding `api/routers/astrogrid_helpers.py:22` and `scripts/export_astrogrid_local_data.py:1053`; only the entity-map and route layers above it have tests. Add reference-value tests for `_solve_kepler`, `_ecliptic_to_equatorial`, `_normalize_angle`, `_angular_separation` (deterministic math, ~50 LOC of tests) before touching the planetary positions. **RESOLVED 2026-06-26 (PR #TBD):** `tests/test_ephemeris_solvers.py` covers all four helpers + the adjacent `_signed_angular_diff` (43 parametrised cases): `_solve_kepler` circular-orbit identity / Kepler-equation residual / high-e branch / unnormalized-M; `_ecliptic_to_equatorial` vernal-equinox / summer / winter solstice declination / zero-obliquity identity / RA wrap; `_normalize_angle` modulo-360; `_angular_separation` short-arc + 0-180 bound.
- [ ] [P1] Resolve `research_agent` cross-tree drift — same module name, divergent shapes — `analysis/research_agent.py:1` and `hyperspace/research_agent.py:1` — `analysis/` version is 517 LOC of module-level functions (`analyze_gaps`, `run_full_research`, `fill_missing_stocks`, plus the dead `research_actor`/`research_sector`); `hyperspace/` version is 197 LOC with a single `GRIDResearchAgent` class. Both are imported in prod (`api/routers/flows.py:1787-1803` and `intelligence/scheduler.py:84` reach into `analysis.`; nothing currently imports `hyperspace.research_agent` but its 197 LOC are live). Pick a canonical home (rename the loser to `analysis/gap_research.py` / `hyperspace/grid_research_agent.py`) so a future `from research_agent import …` cannot silently grab the wrong body.
- [x] [P1] Fix stale docstring + decide on dead module `analysis/lead_lag_backtest.py` — `analysis/lead_lag_backtest.py:18` — module docstring instructs callers to do `from analysis.lead_lag_backtest import LeadLagBacktest; bt = LeadLagBacktest(leader_series=[...], follower_series=[...], lag_days=3, ...); result = bt.run()`, but no `LeadLagBacktest` class exists in the file — only `FoldResult`, `WalkForwardResult`, and `run_walk_forward`. Anyone copy-pasting the docstring example crashes at import. Module also has zero prod callers (only `tests/test_lead_lag_backtest.py` imports it). Either rewrite the docstring to match `run_walk_forward(...)` or wire it into `analysis/backtest_scanner.py` as the docstring already advertises ("walk-forward validation layer on top of … `scan_all_pairs`"). **RESOLVED 2026-06-29:** rewrote the Usage example to `from analysis.lead_lag_backtest import run_walk_forward` + kwargs-only call matching the public signature. Wiring into `analysis/backtest_scanner.py` deferred — no prod caller yet so left as a follow-up.
- [ ] [P2] Log instead of silently swallowing in `viz_intelligence.compute_source_weights` engine acquisition — `analysis/viz_intelligence.py:517` and `analysis/viz_intelligence.py:535` — both `except Exception: pass` blocks wrap the `db.get_engine()` resolution and the `source_catalog` freshness query that drives visual-weight decay; on failure every weight silently falls back to the static schedule with no operational signal. Per `CLAUDE.md` log-level guidance, convert to `log.warning("viz freshness query failed", err=str(exc))` so it shows up in `errors.jsonl`.
- [ ] [P2] Log instead of silently swallowing in `money_flow_engine/layer_credit._money_market_funds_node` — `analysis/money_flow_engine/layer_credit.py:162` — `except Exception: pass` wraps the FRED `MMMFFAQ027S` lookup; on failure the money_market_funds node ships with the prior default value and `confidence="estimated"` but the operator never learns the FRED hit failed. Convert to `log.warning`.
- [ ] [P2] Delete 5 unused public symbols from `analysis/transfer_entropy.py` (appears unused, verify before removing) — `analysis/transfer_entropy.py:57` — `TransferEntropyResult` (57), `LeadLagScan` (87), `pair_transfer_entropy` (196), `discover_leaders` (258) have zero callers anywhere (prod, tests, or sibling analysis modules); only `quantile_discretize`, `transfer_entropy`, and `scan_lead_lag` are referenced (transfer_entropy + scan_lead_lag are exercised by `tests/test_transfer_entropy.py` and `tests/test_transfer_entropy_canonical.py`). Cleanup is ~150 LOC and one comment update at `physics/transforms.py:626`.
- [ ] [P2] Decide on the 4 dead lookup helpers in `analysis/market_universe.py` — `analysis/market_universe.py:1111` — `get_universe` (1111), `get_sector` (1116), `get_industry` (1128), `get_peers` (1141) are listed in the module docstring as the public API but have zero callers; the prod consumers (`intelligence/market_edge_scanner.py:15`, `intelligence/deep_graph.py:204`, `api/routers/watchlist_analysis.py:36`) only reach for `MARKET_UNIVERSE`, `get_all_tickers`, `search_company`. Either wire them into `api/routers/watchlist_analysis.py` (which already imports from this module) or delete — ~70 LOC + a docstring update.
- [ ] [P2] Make `analysis/viz_intelligence.py` `DataShape` / `RelationType` / `WeightSchedule` private or wire them through `api/routers/viz.py` — `analysis/viz_intelligence.py:77` — three public types with zero external imports; `api/routers/viz.py:14` pulls only `ChartType`, `VizSpec`, `AnimationConfig`, `WEIGHT_SCHEDULES`, `VISUALIZATION_RULES`, `compute_source_weights`, `get_all_rules`, `select_visualization`. `WeightSchedule` is the value-type of `WEIGHT_SCHEDULES`, so it's effectively private; `DataShape` and `RelationType` look like dead leftovers from an earlier shape-aware visualization design. Either prefix with `_` or expose via the router so the typing system is honest about who can use them.

## Auditor 2026-06-28 — tests/

Metrics: 427 py files, 114,876 total LOC, 38 commits in last 30d (heavy churn — area is actively absorbing the punch-list test-coverage backlog).
Checks run: A B C D E F G H I J (J = test-quality + DB-mock vs real-PG + `assert True`)

- [ ] [P1] Stop hardcoding the `pg_engine` DSN with the `changeme` placeholder in `tests/conftest.py` — `tests/conftest.py:17` — the fixture builds `create_engine("postgresql://grid_user:changeme@localhost:5432/grid")` and ignores `config.py` entirely. The literal password is the same value that `config.py:548`'s `DB_PASSWORD` validator rejects in non-development environments per `.claude/rules/security.md` ("`config.py` enforces a non-`changeme` DB password outside `development` env"). All 12 integration test files (`test_acquisition_decomposition.py`, `test_capital_flow_rollups.py`, `test_canvas_predict.py`, `test_goal_queue.py`, `test_holder_deal_overlap.py`, `test_hypothesis_kills.py`, `test_intelligence_search.py`, `test_journal.py`, `test_pit.py`, `test_reasoning_bank.py`, `test_remote_access_ops_artifacts.py`, `test_resolver.py`) that depend on `pg_engine` cannot target any non-localhost Postgres without editing source, and any env-configured app DB (`GRID_DB_HOST` / `GRID_DB_USER` / `GRID_DB_PASSWORD`) is silently bypassed. Swap to `from config import settings; engine = create_engine(settings.DB_URL, pool_pre_ping=True)` so the fixture honors the same env-driven DSN as `api/main.py`.
- [ ] [P2] Rename six pytest fixtures whose names start with `test_` to drop the pytest collection-collision smell — `tests/test_journal.py:16` (`test_engine`), `tests/test_resolver.py:990` (`test_engine`), `tests/test_pit.py:23` (`test_engine`), `tests/test_security.py:100` (`test_client`), `tests/test_acquisition_decomposition.py:49` (`test_actor_id`), `tests/test_capital_flow_rollups.py:58` (`test_actor_id`) — pytest's name-based discovery sees `test_*` callables; the `@pytest.fixture` decorator suppresses collection but emits a `PytestCollectionWarning` and the names confuse readers (a parameter signature `def test_journal_insert(test_engine)` reads as "this test takes another test as input"). Rename to `journal_engine` / `resolver_engine` / `pit_engine` / `client` / `actor_id` and update each file's ~5–30 in-file references (no cross-file impact — these are file-local fixtures).
- [ ] [P2] Replace `assert True` in `test_intelligence_search.py` with a no-raise comment or remove it — `tests/test_intelligence_search.py:477` — the test executes `REFRESH MATERIALIZED VIEW CONCURRENTLY intelligence_search` and then `assert True` to "lock in" no-exception; per the audit-area-specific rule for `tests/` (`assert True` is dead code), drop the line and let the SQLAlchemy call raise on failure. One-line edit.
- [ ] [P2] Move `tests/contracts/test_trust_handler.py` off in-memory sqlite onto the shared `pg_engine` fixture — `tests/contracts/test_trust_handler.py:16` — fixture creates `sqlite:///:memory:` and then monkey-patches `dbapi_conn.create_function("GREATEST", 2, max)` (line 20) because sqlite has no `GREATEST`. The production handler `contracts/handlers/trust.py` runs against PostgreSQL with real `GREATEST(...)`, `ON CONFLICT`, and trust-score numeric types; sqlite + a Python `max` callback masks dialect divergence (e.g., `GREATEST(NULL, 0.5)` returns 0.5 in PG but None via this `max` shim). The repo already ships a Postgres-skipping `pg_engine` fixture in `tests/conftest.py:21` that's the right substrate for these handler integration tests; switch and drop the sqlite scaffolding.
- [ ] [P2] Extract a single canonical `FakeEngine` to `tests/_fakes.py` and migrate the three highest-traffic copies — `tests/test_chain_contagion.py:151`, `tests/test_confidence_bucket_tracker.py:68`, `tests/test_alpha_research_signals.py:58` — there are 75 separate `FakeEngine` / `_FakeEngine` / `FakeConn` / `_FakeConn` definitions across `tests/*.py`, each re-implementing the same `.connect()` / `.begin()` / `.execute()` context-manager shape. Conftest only exposes `mock_engine` (a `create_autospec(Engine)` for trivial mocks) — the substring-routed FakeConn pattern that ~30 files lean on has no shared base. Introduce `tests/_fakes.py` with `FakeEngine`, `FakeConn`, `FakeResult` and migrate the three files above (each is one local class; no cross-file impact). Subsequent migrations become single-PR-sized follow-ups.

## Auditor 2026-07-09 — scripts/

Metrics: 197 py files, 71,193 total LOC, 9 commits mentioning scripts in last 30d.
Checks run: A B C D E F G H I J (J = shell-injection surface + orphan research spikes + hardcoded server paths)

- [ ] [P1] Rewrite the shell-decompress call in `scripts/download_pm_data.py` to argument-list form — `scripts/download_pm_data.py:65` — the fallback branch runs `subprocess.run(f"zstd -d {archive_path} --stdout | tar -xf - -C {data_dir}", shell=True, check=True)` where both `archive_path` and `data_dir` derive from the `--data-dir` CLI arg (defaulted to `Path(__file__).resolve().parents[1] / "data" / "prediction_markets"`). A user (or a symlink-chase in `resolve()`) that produces a path with a space, semicolon, or backtick either breaks the pipeline or executes attacker-controlled shell. Replace with a `Popen`-piped pair: `p1 = subprocess.Popen(["zstd", "-d", str(archive_path), "--stdout"], stdout=subprocess.PIPE); subprocess.run(["tar", "-xf", "-", "-C", str(data_dir)], stdin=p1.stdout, check=True); p1.wait()`. Same file already uses argument-list form for `curl`/`wget` — mirror that pattern.
- [ ] [P1] Escape or parameterize the Cypher payload in `scripts/age_fast_sync.py` — `scripts/age_fast_sync.py:50` — the `MERGE (a:Actor …)` block interpolates `_esc(name)`, `_esc(title)`, `_esc(cat)`, `_esc(cred)` into a Cypher literal that is then wrapped in a `$$ … $$` dollar-quoted string and fed to `SELECT * FROM cypher('grid_graph', $$ … $$)`. `_esc` (defined at line 15) escapes only `\` and `'` — it does not neutralize `$$`, which would close the outer Postgres dollar-quote, nor Cypher `{`/`}` boundaries. Actor rows come from `actors.name`/`title`/`credibility` which are populated from ingestion (`intelligence/actor_registry.py`, wikilink imports, LLM output) — none of these are guaranteed to be `$$`-free. Either use a Cypher parameter block (`cypher('grid_graph', $$ MERGE (a:Actor {actor_id: $aid}) … $$, %s)`) or extend `_esc` to reject `$` characters with a fail-loud assertion. Same issue duplicated at `scripts/age_fast_sync.py:97` (connections MERGE).
- [ ] [P1] Add a test file for `scripts/deploy.py` covering the atomic-rename + SHA256-verify + rollback path — `scripts/deploy.py:1` — 413 LOC, ZERO test coverage, and DEPLOY.md declares this the only sanctioned deploy channel ("Every backend file change must be deployed via `scripts/deploy.py`"). A silent regression to the temp-file-rename or hash-verify logic would restore the ad-hoc-scp drift class that the doc calls out as the root cause of "TOP ASAP FIX #1". Add `tests/test_deploy.py` with fakes for `scp`/`ssh` (patch `subprocess.run`) that exercise: (a) happy dual-write, (b) hash-mismatch → rollback, (c) partial-write on one tree → rollback both, (d) `--snapshot` flag creates `.grid_backups/deploy_TIMESTAMP/`. Keep it under 200 LOC.
- [ ] [P1] Add a test file for `scripts/verify_systemd_units.py` — `scripts/verify_systemd_units.py:1` — 279 LOC, ZERO test coverage; DEPLOY.md describes this as the drift-detection tool between `server_setup/*.service` and live systemd, and its `--fix-direction repo-to-live`/`live-to-repo` flags rewrite unit files on either side. A bug in the parser or key-comparator silently declares drift-free when it isn't (or vice versa). Add `tests/test_verify_systemd_units.py` with fixture `.service` files (in `tests/fixtures/systemd/`) that exercise: (a) exact-match, (b) `ExecStart` divergence, (c) whitespace/quote noise that must NOT count as drift, (d) unit present in repo but missing from live. Mock the `systemctl show` invocation.
- [ ] [P1] Add a test file for `scripts/dispatch_agent.py` covering `compose` and `verify` subcommands — `scripts/dispatch_agent.py:1` — 457 LOC, ZERO test coverage; per the module docstring this is the enforcement point for the AGENT_PROMPT_TEMPLATE preamble + coverage check that the "Agent dispatch policy" rule in CLAUDE.md relies on. A regression that drops the preamble or fails the coverage embed removes the guardrail codebase-wide. Add `tests/test_dispatch_agent.py` covering: (a) `compose` prepends the preamble text loaded from `docs/AGENT_PROMPT_TEMPLATE.md`, (b) `compose` invokes and embeds `scripts/pre_create_check.py` output (mock via `subprocess.run` patch), (c) `verify` parses a valid `<agent-return>` JSON block, (d) `verify` errors on missing required fields.
- [ ] [P2] Add type hints on the 15 public job handlers in `scripts/worker.py` — `scripts/worker.py:239` — `execute_job(job, coordinator_url)`, `drain_finished_futures`, `send_heartbeat`, `claim_next_job`, `run_claimed_job`, `run_hypothesis_test`, `run_regime_detect`, `run_llm_inference`, `run_backtest`, `run_feature_compute`, `run_simulation`, `run_data_pull`, `run_kill_predictor_score`, `run_embedding_batch`, and 5 more at lines 278/291/305/316/361/420/467/481/540/670/722/805/951/1119 all take `job`/`params` dicts and `coordinator`/`worker_id` strings with no annotations. This is the distributed-compute worker; malformed params from a bad coordinator dispatch surface as `KeyError` deep inside the handler instead of at the RPC boundary. Add `dict[str, Any]` (or a `TypedDict` per handler if signatures are stable) + return-type annotations. Single-file, mechanical.
- [ ] [P2] `scripts/connect_dots.py` appears unused, verify before removing — `scripts/connect_dots.py:1` — 308 LOC, zero references in `*.py`/`*.md`/`*.yaml`/`*.service` outside the file itself; `scripts/connect_dots_v2.py` docstring says "Goes deeper than v1: backtests signals, finds hidden chains…", so v1 is superseded. The one apparent match (`intelligence/signal_convergence_scanner.py:11`) points at `feedback_connect_dots.md`, not the script. Confirm no dynamic dispatch (`importlib`, `__import__`, `subprocess python … connect_dots.py`) and delete.
- [ ] [P2] `scripts/sharpe_199_final.py` appears unused, verify before removing — `scripts/sharpe_199_final.py:1` — 356 LOC one-off research spike ("Final push to Sharpe 1.99 — tighten the best configurations."), zero external references. The named target (`Sharpe 1.99`) was a milestone; the script has no CLI wiring, no test, no systemd unit. Confirm no dynamic dispatch and delete.
- [ ] [P2] `scripts/sharpe_199_research.py` appears unused, verify before removing — `scripts/sharpe_199_research.py:1` — 483 LOC sibling research spike to `sharpe_199_final.py`, zero external references. Same disposition — verify and remove.
- [ ] [P2] `scripts/reverse_engineer_10000pct.py` appears unused, verify before removing — `scripts/reverse_engineer_10000pct.py:1` — 341 LOC exploratory "reverse-engineer the 10,000% model" notebook-style spike, zero external references. Confirm no dynamic dispatch and delete.
- [ ] [P2] Split `get_companies` and `get_connections` in `scripts/seed_vip_network.py` — `scripts/seed_vip_network.py:815` — `get_companies` is 1025 lines and `get_connections` at `scripts/seed_vip_network.py:1847` is 1690 lines; each function is a single hardcoded actor-list literal. These bodies belong in per-tier YAML/JSON in `data/seeds/` loaded at runtime, not in Python literals — the current shape is un-diffable, un-reviewable, and blocks running the file through the AST checks the auditor script uses on other scripts. First cut: extract the top 300 lines of each into `data/seeds/vip_companies.yaml` and load via `yaml.safe_load`, leaving the rest for a follow-up.
- [ ] [P2] Split `run_cycle` and `run_intelligence_tasks` in `scripts/hermes_operator.py` — `scripts/hermes_operator.py:1298` — `run_cycle` is 797 lines and `run_intelligence_tasks` at `scripts/hermes_operator.py:499` is 708 lines; the file is 2347 LOC total. Both functions inline the per-step sequencing that should be `steps: list[Callable]` iterated in a small loop. Extract the top 3–5 obvious sub-steps (each is a `try/except` block with logging) into module-level functions; keep the change under 200 LOC by leaving the tail for follow-ups.
- [ ] [P2] Split `scripts/seed_vip_network.py` — `scripts/seed_vip_network.py:1` — 3757 LOC single file mixing billionaires (line 35), world leaders (line 576), companies (line 815), connections (line 1847), and upsert helpers (line 3544). Move the four data-registry blocks into `scripts/seeds/vip_billionaires.py`, `scripts/seeds/vip_world_leaders.py`, `scripts/seeds/vip_companies.py`, `scripts/seeds/vip_connections.py`, and keep `seed_vip_network.py` as a thin `main()` that imports and orchestrates. Under 200 LOC changed by moving one block per PR.
