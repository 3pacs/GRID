# Audit A — Fabricated data in the intelligence router surface

Repo: `C:\Users\owner\dev\GRID-wt-fake-data-audit` @ `audit/fake-data-cleanup-20260917` (= origin/main `fe6765bc`, deployed as `grid-api` on grid-svr).
Scope: the 22 named routers under `api/routers/`, plus helper modules in `intelligence/`, `analysis/`, `store/` followed where a response value's origin was unclear.
Read-only audit. No files were modified.

Categories: **1 = FABRICATED FALLBACK**, **2 = PLACEHOLDER METRIC**, **3 = MISLEADING PROVENANCE**, **4 = HARDCODED VALUES PRESENTED AS OBSERVATIONS**.

---

## Findings table

### HIGH

| # | File:line | Snippet | Endpoint(s) | Cat | Conf. | Honest replacement |
|---|---|---|---|---|---|---|
| H1 | `intelligence/institutional_map.py:49`, `:257`, `:436`, `:713`, `:806`, `:854`, `:903`, `:933` (fn never touches `engine`) | `PRIVATE_CREDIT_FUNDS = { "apollo": { "aum": 908_000_000_000, "credit_aum": 723_000_000_000, "key_personnel": [{"name": "Marc Rowan", "net_worth_est": 8_200_000_000}], "confidence": "confirmed" } … }` | `GET /api/v1/intelligence/institutional-map`, `/institutional-map/trace/{pension}`, `/institutional-map/fees/{fund}`, `/institutional-map/conflicts` (routers `intelligence_companies.py:292-395`) | 4 + 3 | CONFIRMED — `build_institutional_graph(engine)` accepts an engine and never opens a connection; `get_institutional_summary()`, `trace_pension_dollars()`, `get_fee_extraction_estimate()`, `find_conflicts_of_interest()` take no engine at all | The whole family should either be sourced from a table with real `as_of` dates per row, or the response must carry `"data_source": "static_curated"` plus a per-record `as_of` and the endpoints should return `{"available": false, "reason": "no live institutional data"}` for anything not backed by a filing. |
| H2 | `intelligence/institutional_map.py:1503-1507` | `"private_credit_crisis_2026": {"apollo_redemption_cap": "March 23, 2026", "blue_owl_fire_sale": "$1.4B (February 2026)", "fortune_headline": "$265B private credit meltdown"}` | `GET /api/v1/intelligence/institutional-map` (`summary`) | 4 | CONFIRMED | Remove, or move behind a `curated_notes` key with an explicit "hand-entered, not verified from a feed" label. |
| H3 | `intelligence/institutional_map.py:1144`, `:1337`, `:1343` | `est_gross_return = amount * 0.08` / `assumed_gross_return = 0.08` / `est_passthrough = total_pension_capital * 0.02 if passthrough else 0` | `/institutional-map/trace/{pension}`, `/institutional-map/fees/{fund}` | 2 | CONFIRMED | Fee projections built on an assumed 8% gross return should be returned as `null` with `"reason": "no realized-return data"`, or the assumption must be echoed in the payload as `assumptions: {gross_return_pct: 8.0, source: "assumed"}`. |
| H4 | `intelligence_risk.py:1023-1034` | `CAPITAL_PAIRS = [("JPN","USA",200_000_000_000), ("CHN","USA",150_000_000_000), …]` then `for from_id, to_id, est_vol in CAPITAL_PAIRS: flows.append({"type":"capital","volume": est_vol, "direction":"inflow"})` | `GET /api/v1/intelligence/globe` | 4 | CONFIRMED — these are appended unconditionally, no DB read anywhere in that block | Drop the capital arcs entirely unless a `capital_flows` table backs them; otherwise return `"capital_flows": []` with `"capital_flows_available": false`. |
| H5 | `intelligence_risk.py:1016-1021` | `except Exception: for from_id, to_id in TRADE_PAIRS: flows.append({"type":"trade","volume": 50_000_000_000, "direction":"surplus","change_1y": None})` | `GET /api/v1/intelligence/globe` | 1 | CONFIRMED | On Comtrade failure return `"flows": []` and `"errors": ["trade_flows: <exc>"]` — never a synthetic $50B arc per pair. |
| H6 | `intelligence_risk.py:1010` | `"volume": volume or 50_000_000_000,` | `GET /api/v1/intelligence/globe` | 1 + 2 | CONFIRMED | `"volume": None` and let the globe render the arc as unsized/greyed. |
| H7 | `intelligence_risk.py:324-326` → `:339-345` | `hy_val = float(hy_row[0]) if hy_row else 400` / `ig_val = … else 100` / `ted_val = … else 0.3` … `credit_risk = {"hy_spread": round(hy_val), "ig_spread": round(ig_val), "ted_spread": round(ted_val,2), …}` | `GET /api/v1/intelligence/risk-map` | 1 + 2 | CONFIRMED | Missing series → `{"hy_spread": null, …, "risk_level": "unknown", "unavailable_reason": "no hy/ig/ted series in resolved_series"}`; do not contribute a score to `overall_risk_score`. |
| H8 | `intelligence_risk.py:134`, `:144`, `:147`, `:156-162` | `vix_val = float(vix_row[0]) if vix_row else 20.0` … `pct = 50` … `vol_risk = {"vix": round(vix_val,1), "vix_percentile_1y": pct, …}` | `GET /api/v1/intelligence/risk-map` | 1 + 2 | CONFIRMED | A VIX of 20.0 and a 50th percentile with no rows behind them are indistinguishable from real readings. Return `null` + `"risk_level": "unknown"`. |
| H9 | `intelligence_risk.py:153` | `rv_iv = min(round(vix_val / max(vix_val * 0.9, 1), 2), 2.0)` → served as `"realized_vs_implied"` | `GET /api/v1/intelligence/risk-map` | 2 | CONFIRMED — algebraically this is always ≈1.11 for any vix_val > ~1.1; no realized-vol series is ever read | Compute realized vol from an actual return series, or return `"realized_vs_implied": null` with `"reason": "realized vol not computed"`. |
| H10 | `intelligence_risk.py:53-61`, `:116-122`, `:169-174`, `:222-226`, `:284-290`, `:352-357` | `dealer_risk = {"gex_regime":"unknown","net_gex":0,"gamma_flip_distance_pct":0,"days_to_opex":30,"risk_level":"moderate"}` (same shape for vol/conc/corr/credit/liq) | `GET /api/v1/intelligence/risk-map` | 1 | CONFIRMED — these literals are what ships whenever the sub-system `except` fires; only `errors[]` hints at it | Each failed sub-system should serialize as `{"risk_level":"unknown","available":false,"reason":"<exc>"}` with no numeric fields. |
| H11 | `intelligence_risk.py:510-522` | `except Exception: return {"dealer_risk": {"risk_level":"moderate"}, …, "overall_risk_score": 0.5, "risk_narrative": f"Risk map computation error: {exc}"}` | `GET /api/v1/intelligence/risk-map` | 1 | CONFIRMED | HTTP 503 or `{"available": false, "error": …}` — a total failure must not render as "moderate risk across the board, score 0.5". |
| H12 | `intelligence/export_intel.py:72-112` (`_CHINA_REVENUE_ESTIMATES`) + `:376-400` | `"NVDA": {"china_revenue_pct": 25.0, "total_revenue_b": 60.9,  # FY2024 approximate …}` … `at_risk_pct = base_pct * 0.85` / `* 0.5` / `* 0.25` / `* 0.1` / `* 0.05`, then `* 1.2` / `* 1.1` | `GET /api/v1/intelligence/export-controls/impact`, `GET /api/v1/intelligence/export-controls?ticker=…` (`revenue_impact`) | 4 + 2 | CONFIRMED | `china_revenue_pct` / `total_revenue_b` must come from filings with an `as_of` fiscal period, and `estimated_revenue_at_risk_pct` must be labeled `"basis": "heuristic_multiplier"` — or the endpoint should return `risk_level: "UNKNOWN"` rather than a number derived from an FY2024 literal in 2026. |
| H13 | `intelligence/actors/seed_data.py:17-…` (5,618 lines, 333 `net_worth_estimate`/`aum` literals) + `intelligence/actors/db.py:82-116` + `intelligence/actors/graph.py:188-190` | `"fed_powell": {"influence_score": 0.99, "credibility": "hard_data", …}` ; `"net_worth_estimate": 15_500_000_000, "aum": 124_000_000_000` ; `if not actors: _seed_known_actors(engine); actors = _load_actors_from_db(engine, …)` | `GET /api/v1/intelligence/actor-network`, `/actor/{id}`, `/grand-power-map`, `/ego-graph/{id}`, `/power-map/{sector}`, `GET /api/v1/intel/actor/{name}`, `/intel/search`, `/intel/entity/{name}` | 4 + 3 + 1 | CONFIRMED — the seed upsert is a live fallback, not a fixture; it also writes `updated_at = NOW()` (`db.py:115`) so hand-entered figures appear freshly refreshed | Seeded rows need a `provenance='seed'` column surfaced as `"source": "curated_seed"` with the real vintage of the figure; `influence_score` should be `null` unless computed; `credibility: "hard_data"` must not be attached to a hand-assigned score. |
| H14 | `intelligence/market_edge_scanner.py:144-145` (and 9 more playbooks) + `:1070`, `:1095`, `:1105-1109` | `base_confidence=74, base_edge=18` … `confidence = playbook.base_confidence` … `expected_edge = max(10, playbook.base_edge + max(0, (confidence - 58) // 7))` … `"expected_edge_pct": int(expected_edge), "score": confidence, "confidence": confidence` | `GET /api/v1/intelligence/edges` (`intelligence_edges.py:17-25`) | 2 | CONFIRMED | `expected_edge_pct` is a percentage return with no backtest behind it. Either drop it or rename/relabel as `"heuristic_rank"` with `"basis": "playbook_prior"`; `confidence` should expose its components rather than a tuned constant. |
| H15 | `intelligence/dollar_flows.py:58`, `:184`, `:361-363` | `_DEFAULT_VWAP_ESTIMATE: float = 50.0` … `return _DEFAULT_VWAP_ESTIMATE` … `vwap = _get_vwap_estimate(engine, ticker, obs_date); amount = volume * vwap` → `"amount_usd": amount` | `GET /api/v1/intelligence/dollar-flows` (all shapes), `/actor-network` (`flows`, `flow_summary.total_tracked`), `GET /api/v1/intel/briefing` (`notable_flows` via `wealth_flows`) | 2 + 1 | CONFIRMED (the `evidence.vwap_estimate` field partially discloses it, the top-line `amount_usd` does not) | When no price exists for the ticker/date, emit `"amount_usd": null, "amount_unavailable_reason": "no price series"` instead of `volume × $50`. |
| H16 | `intelligence/dollar_flows.py:684`, `:690` | `estimated_net = value * 0.003` → `"amount_usd": abs(estimated_net)` | `GET /api/v1/intelligence/dollar-flows`, `/actor-network` flows | 2 | CONFIRMED | This is the textbook case (`flow = volume × constant`). Ship `null` or move it to a clearly separate `modeled_flows` block with the multiplier in the payload. |
| H17 | `associations.py:439-448` | `regime_labels = ["GROWTH","NEUTRAL","FRAGILE","CRISIS","RECOVERY","UNKNOWN"]` … `label = regime_labels[i] if i < len(regime_labels) else f"CLUSTER_{i}"` | `GET /api/v1/associations/clusters` | 2 + 4 | CONFIRMED — the label is assigned purely by cluster index; nothing in the clustering result determines which cluster is "CRISIS" | Return `"label": f"CLUSTER_{i}"` and let a separate, explicit mapping (or feature-centroid interpretation) supply semantic names. |

### MED

| # | File:line | Snippet | Endpoint(s) | Cat | Conf. | Honest replacement |
|---|---|---|---|---|---|---|
| M1 | `intelligence/cross_reference.py:285`, `:287` | `official_value=official_value if official_value is not None else 0.0,` / `physical_value=physical_value if physical_value is not None else 0.0,` | `GET /api/v1/intelligence/cross-reference`, `/cross-reference/category/{c}`, `/cross-reference/ticker/{t}`, `GET /api/v1/intel/cross-reference/{indicator}`, `/intelligence/dashboard` (`cross_ref.red_flags`) | 2 | CONFIRMED | `0.0` for a missing official statistic is indistinguishable from an observed zero. Keep `None` and let the check report `assessment: "insufficient_data"`. |
| M2 | `intel.py:1922-1924`, `:1933-1934`, `:1949` | `AVG(COALESCE(ss.trust_score, 0.5)) AS avg_trust` … `"avg_source_trust": avg_trust` | `GET /api/v1/intel/briefing` (`convergence_signals`) | 2 | CONFIRMED | Sources with no trust row should be excluded from the weighting and counted separately (`"untrusted_source_count": n`), not imputed at 0.5. |
| M3 | `intelligence_actors.py:1018-1019`, `:1033`, `:1065`, `:1099`, `:1115-1116`, `:1223`, `:1278-1279`, `:1316`, `:1701-1702`, `:1731`, `:1769`, `:1787-1788`; also `intelligence/actors/graph.py:259`, `:278` | `"influence": float(row[3]) if row[3] else 0.5,` / `"trust": float(row[4]) if row[4] else 0.5,` / `"strength": float(r[3]) if r[3] else 0.5,` / `"influence": … if a[4] else 0.3` | `/power-map/{sector}`, `/ego-graph/{id}`, `/ego-graph/search`, `/grand-power-map`, `/actor-network/db`, `/actor-network` | 2 | CONFIRMED | NULL influence/trust/strength should serialize as `null` so the UI can render "unscored"; 0.5/0.3 are indistinguishable from measured mid-range scores. Note the `if x else` form also silently rewrites a genuine `0.0` to `0.5`. |
| M4 | `intelligence_actors.py:1473`, `:1508`, `:1529`, `:1565`, `:1570` | `strength=min(1.0, float(r[4]) if r[4] is not None else 0.6)` … `_add_edge(cmid, anchor, "jurisdiction", strength=0.5)` … `_add_edge(matched, mid, "member_trade", strength=0.55)` … `_add_edge(mid, center_id, "congressional_trade", strength=0.5, …)` | `GET /api/v1/intelligence/intel-expand/{ticker}` | 2 | CONFIRMED | Jurisdiction/member-trade edges have no measured strength — omit the field (or set `null`) rather than shipping 0.5/0.55/0.6, which the canvas renders as edge weight. |
| M5 | `intelligence_actors.py:1024-1042` | `if not actor_map:  # Fallback: build from sector_map data alone (no DB matches)` … `nodes.append({… "influence": a["influence"], "trust": 0.5, …})` then `return {"nodes": nodes, "edges": [], …}` | `GET /api/v1/intelligence/power-map/{sector}` | 1 + 3 | CONFIRMED | The fallback returns a curated YAML list as if it were the sector's power map. Mark every node `"source": "sector_map_curated"` (as the synthetic branch at `:1150` already does) and set `"trust": null`. |
| M6 | `intelligence_actors.py:1136-1151`, `:1161-1163` | `actor_map[synth_id] = {… "influence": a["influence"], "trust": 0.5, "synthetic": True}` … `if name_lower in influence_by_name: node["influence"] = influence_by_name[name_lower]  # Use sector_map influence (more curated)` | `GET /api/v1/intelligence/power-map/{sector}` | 3 | CONFIRMED | Synthetic nodes *are* flagged (good), but line 1161-1163 silently overwrites the DB's measured `influence_score` on **real** nodes with the curated YAML weight and leaves no marker. Add `"influence_source": "curated"\|"db"`. |
| M7 | `analysis/thesis_scorer.py:58`, `:74`, `:101`, `:2483` | `DEFAULT_ACCURACY = 0.50  # start at coin-flip until we have history` … `historical_accuracy: float = DEFAULT_ACCURACY` … `"historical_accuracy": round(historical_accuracy, 3)` | `GET /api/v1/intelligence/thesis` (`models[].historical_accuracy`) | 2 | CONFIRMED | A model with zero scored history reports `historical_accuracy: 0.5`, identical to a model that genuinely went 50/50. Emit `null` + `"scored_history_count": 0`. |
| M8 | `intelligence_causation.py:66`, `:69`, `:71-74`, `:104`, `:108` | SQL `ELSE cl.action_date + 2 END AS effect_date` / `COALESCE(sd.description, 'Price reaction following ' \|\| cl.cause_type \|\| ' signal')` / `COALESCE((cl.evidence->>'lead_time_days')::numeric, 2)`; Python `"probability": float(row[7]) if row[7] is not None else 0.5, … "lead_time_days": … else 2.0` | `GET /api/v1/intelligence/causal-links?ticker=…` | 2 + 4 | CONFIRMED | The D3 timeline draws an arrow to a fabricated `effect_date` (`action_date + 2 days`) with probability 0.5 and a template description. Links without a measured lead time / probability should be returned with `null` fields and rendered as unanchored. |
| M9 | `intelligence_risk.py:943`, `:958` | `activity_score = round(sum(score_parts)/len(score_parts), 2) if score_parts else 0.5` and, in the per-country `except`, `"activity_score": 0.5` alongside `"gdp_signal": "no_data"` | `GET /api/v1/intelligence/globe` | 2 | CONFIRMED | `"activity_score": null` — the globe colours countries by this value, so 0.5 paints "average activity" over a country with no data at all. |
| M10 | `intelligence_risk.py:724`, `:1115` | `snapshot["overall_confidence"] = 0.5` (no components) / dashboard `except` → `"overall_confidence": 0.5, "narrative": f"Dashboard build error: {exc}"` | `GET /api/v1/intelligence/dashboard` | 1 + 2 | CONFIRMED | `null` with `"confidence_components": 0`; the narrative generator at `:730-741` currently prints "System confidence is MODERATE at 50%" off a pure placeholder. |
| M11 | `intel.py:198`, `:237`, `:274`, `:321`, `:347`, `:371`, `:385`, `:393`, `:508`, `:534`, `:609`, `:637`, `:699`, `:723`, `:751`, `:773`, `:798`, `:1015`, `:1040`, `:1050`, `:1078`, `:1091`, `:1228`, `:1306`, `:1584`, `:2011`, `:2134` | `"confidence": "confirmed",  # ICIJ is primary source` / `"confidence_label": "confirmed"` / `"confidence": "derived"` — written as literals per code path, never derived from the row | `GET /api/v1/intel/search`, `/intel/entity/{name}`, `/intel/actor/{name}`, `/intel/ticker/{sym}`, `/intel/market-brief`, `/intel/predictions/*`, `/intel/network/{entity}`, `/intel/briefing` | 3 | CONFIRMED | The product's own contract is "confidence labels on all data points" (module docstring, `intel.py:1-9`); today the label reflects which `if` branch ran, not the data. Derive from the row's own provenance column, or rename to `source_class`. |
| M12 | `intel.py:1859` | `"confidence_label": "confirmed" if evt_dict.get("confidence", 0) > 0.7 else "derived",` | `GET /api/v1/intel/briefing` (`lever_events_24h`) | 3 | CONFIRMED | A model confidence over an arbitrary 0.7 threshold is not source confirmation. Report the numeric confidence only. |
| M13 | `intelligence/global_levers.py:49-…` (LEVER_HIERARCHY, ~1,430 lines) | `"fed": {"influence": 1.0, "key_personnel": [{"name": "Jerome Powell", "title": "Chair", "influence": 0.99}, …], "confidence": "hard_data"}` | `GET /api/v1/intelligence/levers`, `/levers/{domain}`, `/levers/cross-domain`, `/levers/chain/{event}` | 2 + 3 | CONFIRMED (`get_lever_hierarchy` deep-copies the constant and only *adds* live metrics; `has_live_data` flags the live part but not the static scores) | Hand-assigned `influence` values labeled `"confidence": "hard_data"` should be re-labeled `"confidence": "curated_estimate"`, and the personnel roster needs an `as_of` (the roster is already drifting from the current board). |
| M14 | `intelligence/dollar_flows.py:370`, `:691` | `"direction": "inflow",  # Dark pool direction is ambiguous` / `"direction": "inflow",  # volume-based proxy` | `GET /api/v1/intelligence/dollar-flows`, `/actor-network` | 3 | CONFIRMED | Direction is unknown for these sources — emit `"direction": null` or `"unknown"` rather than defaulting every dark-pool and ETF row to "inflow", which biases `get_aggregate_flows` net totals. |
| M15 | `associations.py:245-248`, `:375-376` | `corr_values = [[0.0 if (v != v) else round(v,4) for v in row] for row in corr_values]` / `if cc != cc: cc = 0.0` | `GET /api/v1/associations/correlation-matrix`, `/associations/lag-analysis` | 2 | CONFIRMED | An undefined correlation (zero-variance or no overlap) is rendered as a measured zero correlation. Serialize `null` and let the heatmap render an empty cell. |
| M16 | `associations.py:446-447` | `"feature_count": cluster_result.get("pca_components_used", 0), "persistence": best_metrics.get("gmm_persistence", 0) if best_metrics else 0` | `GET /api/v1/associations/clusters` | 3 + 2 | CONFIRMED | `feature_count` is actually the PCA component count (not features), and the *same* whole-model `gmm_persistence` is attached to every cluster as if per-cluster. Rename to `pca_components` and either compute per-cluster persistence or move it to the top level. |
| M17 | `intelligence_spider.py:470-481` | `actor_data = {… "influence_score": 0.5, "trust_score": 0.5, "degree": 0, "source": "operator", "credibility": "rumor"}` then `save_actor(engine, actor_id, actor_data)` | `POST /api/v1/intelligence/spider/inject` (write path; the row is then read back by every actor endpoint) | 2 | CONFIRMED | Persist `NULL` influence/trust for operator-injected actors so downstream endpoints can distinguish "unscored" from "scored 0.5". |
| M18 | `intelligence_actors.py:631` | `confidence_in_analysis=full.get("confidence_in_analysis", 0.5)` | `GET /api/v1/intelligence/postmortems/lessons` (feeds the synthesis) | 2 | PLAUSIBLE — the 0.5 is consumed by `generate_lessons_learned`, not emitted directly, so it shapes prose rather than a displayed number | Skip records with no stored confidence rather than imputing 0.5. |

### LOW

| # | File:line | Snippet | Endpoint(s) | Cat | Conf. | Honest replacement |
|---|---|---|---|---|---|---|
| L1 | `intel.py:1858`, `:1951`, `:1982`, `:2010`, `:2041-2044` | `"invalidation": f"Reversed if {puller.get('name','actor')} closes position within 5 days"` / `"invalidation": "Flag clears when z-score drops below 2.0"` / `"invalidation": "Score drops below 4.0 or IV normalizes above 50th pct"` | `GET /api/v1/intel/briefing` | 2 | CONFIRMED | The docstring promises "an invalidation condition where available"; these are templates with invented thresholds (5 days, z<2.0, score<4.0). Either store a real invalidation with the signal or omit the key. |
| L2 | `intel.py:96-139` | `_VALVE_MAP = {"congress": {"BUY": "legislative-insider flow valve opens — capital entering ahead of policy action"}, …}` → `"valve": valve` | `GET /api/v1/intel/briefing` (`lever_events_24h`) | 3 | CONFIRMED | Canned interpretive prose presented as per-event analysis. Harmless if labeled — add `"valve_source": "template"`. |
| L3 | `actor_detail.py:464-490` (via `api/routers/flows.py:964`, `:1044`, `:1073`) | `result["description"] = a.get("evidence")` / `result["known_holdings"] = a.get("targets", [])` from `_ACTIVIST_HOLDERS` / `_REGULATOR_THREATS` / `_GLP1_PRESSURE` | `GET /api/v1/actors/{actor_id}/detail` | 4 | CONFIRMED | The underlying maps are explicitly documented as curated (`flows.py:959-960`) and the response does expose them under `static_context`, but promoting `evidence` → `description` and `targets` → `known_holdings` strips that marker. Keep the static values only inside `static_context`. |
| L4 | `intelligence_thesis.py:89-103` | `except Exception: return {"overall_direction": "NEUTRAL", "conviction": 0, … "narrative": f"Thesis generation failed: {exc}", "error": str(exc)}` | `GET /api/v1/intelligence/thesis` | 1 | CONFIRMED | Borderline — the `error` field and narrative do disclose the failure, but `"overall_direction": "NEUTRAL"` still reads as a market call. Prefer `"overall_direction": null`. |

---

## Ruled-out constants (considered, excluded)

- `intelligence_risk.py:25-40` — `_compute_risk_level()` / `_level_to_score()` band thresholds (0.85/0.65/0.45/0.25). Classification thresholds, not data.
- `intelligence_risk.py:826-851` (`COUNTRY_CONFIG`), `:863-868` (`FX_PAIRS`), `:962-967` (`TRADE_PAIRS`) — series-id/ticker routing tables. Configuration, not observations. (The *volumes* attached to TRADE/CAPITAL pairs are H4–H6.)
- `intelligence_actors.py:899-931` — `_RELATIONSHIP_COLORS`. Pure UI palette.
- `intelligence_actors.py:30-33` — `_DEFAULT_SECTORS` priority list. A display filter.
- `analysis/sector_map.py` + `analysis/sector_map_data.yaml` — 20 sectors / 3,533 actors with curated `weight` values. Documented as an extracted curated ontology (`sector_map.py:1-28`); excluded as a static map. Its *leakage into measured fields* is M5/M6.
- `api/routers/flows.py:959-960` — "Static power/control maps. V1 is hardcoded because these relationships don't move daily and auditability matters more than dynamism." Documented curated map; excluded (see L3 for the one place the label is lost).
- `ingestion/altdata/legislation.COMMITTEE_SECTOR_MAP` (used by `intelligence_actors.py:1394`) — committee→sector jurisdiction ontology, documented as such.
- `intelligence/dollar_flows.py:44-55` — congressional disclosure amount bands ("F": 500_001–1_000_000 …). These are the statutory disclosure brackets, i.e. real source semantics.
- `associations.py:42-79` — `_SEMANTIC_GROUPS` / `_FEATURE_TO_GROUP`. Feature-family ontology used only to classify pairs as trivial/expected/interesting.
- `associations.py:100-101` (`suffixes`), `:559` `sigma_threshold` default, `:621` `0.5` correlation-break threshold, `:639` extreme/moderate cutoff — detection thresholds, exposed as such.
- `intelligence_search.py:24` `_VALID_TYPES`, `user_intel.py:33-38` `VALID_TYPES`/`VALID_CONFIDENCE`/`RATE_LIMIT_PER_HOUR`, `intel.py:30-34` `Tier` enum — enums and limits.
- `intelligence_spider.py:57-82` — graph load caps and refresh interval from env. Operational config.
- `intelligence_regime.py:92-99` — `TICKERS` / `HORIZONS` for the TimesFM comparison; the block returns `{"available": false, "reason": …}` honestly when the model is missing.
- `analysis/thesis_scorer.py` per-model confidence formulas (e.g. `:1266` `min(70, 15 + total*0.5 + accuracy*15)`) — heuristic model weights, but each verdict ships its own `score`, `confidence`, `data_point`, `reasoning`, `data_age_hours` and a `status` of `active`/`stale`/`no_data`/`broken`, so the construction is visible to the caller. Only the `historical_accuracy` default is called out (M7).
- `intelligence/market_edge_scanner.py:18-41` — `SOURCE_WINDOWS_DAYS`, `SOURCE_LABELS`, `SOURCE_PRIORITY`; `:43-66` `EXCLUDED_TICKERS`; `:69-75` `MANUAL_TICKER_META`. Lookback/labelling config and a one-entry metadata patch.
- `intelligence/market_edge_scanner.py:1338-1344` — `engine is None` → empty `opportunities`. This fallback is honest (it is the `/edges` degraded path); the problem is the scoring constants (H14), not the fallback.
- `intel_source_audit.py:33-39`, `:81`, `:120` and all of `intelligence_news.py`, `intelligence_govflow.py`, `intelligence_search.py`, `intelligence_regime.py`, `attributions.py`, `actor_news_api.py`, `user_intel.py` — every failure path returns empty collections with zero counts plus an explicit `error`/`reason`; `actor_news_api.py` even returns `"available": false`. These are the pattern the rest of the surface should follow.
- No `random`, `numpy.random`, seeded generator, `sample_*`, `demo_*`, `mock_*` or `fake_*` construct exists anywhere in the 22 routers or in the helper modules followed (`market_edge_scanner`, `institutional_map`, `global_levers`, `export_intel`, `dollar_flows`, `intelligence/actors/*`, `thesis_scorer`). All fabrication here is literal-table or constant-arithmetic, not RNG.

---

## Files reviewed (full read)

| File | Lines |
|---|---|
| `api/routers/intelligence.py` | 59 |
| `api/routers/intelligence_actors.py` | 1867 |
| `api/routers/intelligence_causation.py` | 138 |
| `api/routers/intelligence_companies.py` | 427 |
| `api/routers/intelligence_deepdive.py` | 295 |
| `api/routers/intelligence_edges.py` | 30 |
| `api/routers/intelligence_forensics.py` | 460 |
| `api/routers/intelligence_govflow.py` | 324 |
| `api/routers/intelligence_news.py` | 694 |
| `api/routers/intelligence_regime.py` | 223 |
| `api/routers/intelligence_risk.py` | 1119 |
| `api/routers/intelligence_search.py` | 189 |
| `api/routers/intelligence_spider.py` | 486 |
| `api/routers/intelligence_thesis.py` | 565 |
| `api/routers/intel.py` | 2204 |
| `api/routers/intel_cross_reference.py` | 285 |
| `api/routers/intel_source_audit.py` | 120 |
| `api/routers/actor_detail.py` | 525 |
| `api/routers/actor_news_api.py` | 181 |
| `api/routers/associations.py` | 663 |
| `api/routers/attributions.py` | 52 |
| `api/routers/user_intel.py` | 389 |
| **Total (routers)** | **11,295** |

## Helper modules followed (targeted, not full-file)

Read enough of each to establish the origin of the response values cited above; **not** audited end-to-end.

| File | Lines | What was read |
|---|---|---|
| `intelligence/market_edge_scanner.py` | 1356 | Playbook blueprints, `_base_opportunity`, `_build_coverage_gaps`, `_build_summary`, `_build_stakes`, `build_market_edge_snapshot` |
| `intelligence/institutional_map.py` | 1508 | Module docstring, all six data constants' shape, `build_institutional_graph`, `trace_pension_dollars`, `get_fee_extraction_estimate`, `get_institutional_summary`; verified `engine` is never used |
| `intelligence/global_levers.py` | 2305 | Docstring, `LEVER_HIERARCHY` head, `get_lever_hierarchy`, function inventory |
| `intelligence/export_intel.py` | ~600 | `_CHINA_REVENUE_ESTIMATES`, `get_controls_for_ticker` tail, `assess_revenue_impact` in full |
| `intelligence/dollar_flows.py` | ~1000 | Amount bands, `_get_vwap_estimate`, dark-pool normalizer, ETF-flow normalizer |
| `intelligence/actors/seed_data.py` | 5618 | Header + representative entries; counted 333 `net_worth_estimate`/`aum` literals |
| `intelligence/actors/db.py` | 322 | `_seed_known_actors` upsert |
| `intelligence/actors/graph.py` | 417 | `build_actor_graph` node/link assembly and the seed fallback |
| `intelligence/cross_reference.py` | 1977 | `_compute_confidence`, `_make_check`, defaults grep |
| `analysis/thesis_scorer.py` | 2745 | Constants, `_verdict`, `score_thesis` aggregation, confidence-formula grep |
| `analysis/sector_map.py` | 140 | Full |
| `api/routers/flows.py` | — | `_ACTIVIST_HOLDERS` / `_REGULATOR_THREATS` / `_SUPPLY_CHAIN` / `_GLP1_PRESSURE` definitions only |

## Files NOT fully reviewed

- All 22 target routers were read in full. No router was skipped.
- The helper modules in the table above were read only along the paths that produce the flagged response values. In particular `analysis/thesis_scorer.py` (2,745 lines, ~20 model scorers), `intelligence/global_levers.py` (2,305 lines of hierarchy data), `intelligence/cross_reference.py` (1,977 lines, ~8 check families), `intelligence/news_impact.py` (`DeepDiveEngine` — source of `estimated_bps` / `baked_in_pct` / `historical_hit_rate` on `/deep-dive/{ticker}`), `intelligence/deep_graph.py`, `intelligence/lever_pullers.py`, `intelligence/pattern_engine.py`, `intelligence/trust_scorer.py`, `intelligence/sleuth.py` and `store/graph.py` were not exhaustively audited. `/deep-dive/{ticker}` and `/intel/deep-dive/{ticker}` return per-catalyst `estimated_bps` and `confidence` figures whose derivation lives entirely in `news_impact.py` — that module is the most likely remaining source of placeholder metrics and is the recommended next target.
