# ============================================================
# GRID — Python Dependencies
# Install with: pip install -r requirements.txt
# ============================================================

# Database
psycopg2-binary>=2.9.12
sqlalchemy>=2.0.0
alembic>=1.13.0

# Data
aiohttp>=3.9
pandas>=2.1.0
numpy>=1.26.0
fedfred>=1.2.9
yfinance>=0.2.36
requests>=2.31.0
beautifulsoup4>=4.14.3
edgartools>=4.6.3

# Analysis
scikit-learn>=1.4.0
scipy>=1.12.0
statsmodels>=0.14.0
tsfresh>=0.20.0

# Graph analytics
networkx>=3.2.0
python-louvain>=0.16

# Visualization (for orthogonality audit outputs)
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
python-dotenv>=1.0.0
schedule>=1.2.0
pydantic>=2.13.4
pydantic-settings>=2.1.0
eval_type_backport>=0.2.0
loguru>=0.7.2

# Hyperspace (OpenAI-compatible API client) + Hermes analyst bridge
# (intelligence/hermes/ relies on the reasoning-token usage fields)
openai>=1.40.0

# LLM observability — pipes every LLM call into Langfuse for trace/eval/regression
# Self-hosted at http://grid-svr:3000; configured via LANGFUSE_HOST/PUBLIC_KEY/SECRET_KEY env
langfuse>=3.0.0

# International data
akshare>=1.14.0
imfdatapy>=0.1.1
dbnomics>=0.2.6
eurostat>=0.2.1
requests-cache>=1.1.0

# Trade data
comtradeapicall>=0.1.4

# Physical economy
# NOTE: patent-client is intentionally excluded from the base environment.
# It is not used by the current runtime code, and its hishel dependency range
# is incompatible with edgartools, which is used by SEC ingestion.
# If patent-client is needed later, install it in an isolated environment.
boto3>=1.34.0

# Utilities (extended)
tqdm>=4.66.0
tenacity>=8.2.0
websockets>=12.0
pyarrow>=14.0.0
polars>=1.40.1

# ML model training
xgboost>=3.2.0

# TradingAgents multi-agent framework (optional)
# Install separately when the private TradingAgents package is available.
# tradingagents>=0.1.0
# langgraph>=0.2.0
# langchain-openai>=0.1.0
# langchain-anthropic>=0.1.0

# API
fastapi>=0.109.0,<0.137.0
uvicorn>=0.46.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1
httpx>=0.26.0

# Durable event streaming (Redpanda / Kafka)
kafka-python-ng>=2.2.0

# Object storage
minio>=7.2.0

# Workflow orchestration
prefect>=3.7.0

# Testing
pytest>=9.0.3
pytest-timeout>=2.4.0
pytrends>=4.9.0
