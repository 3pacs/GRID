"""
GRID TradingAgents integration.

Runs the TradingAgents multi-agent LLM framework as a parallel signal
source alongside GRID's own inference pipeline.  Agent decisions are
logged to the decision journal for comparison with GRID recommendations.
"""
