# Tier: infra (intelligence loop — briefings, capital flows, options scanner)
# Larger heap than typical workers because it fans many concurrent intel jobs.
[Service]
Slice=grid.slice
MemoryAccounting=yes
MemoryHigh=16G
MemoryMax=24G
TasksMax=8192
OOMScoreAdjust=150
