# Make sshd nearly un-killable by the OOM killer so the box stays reachable
# during a workload memory spike (root cause of the 2026-05-02 incident, where
# the OOM killer ate sshd children and TCP :22 reset at banner-write).
[Service]
OOMScoreAdjust=-900
MemoryAccounting=yes
MemoryMin=64M
