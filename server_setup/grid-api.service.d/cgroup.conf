# Tier: infra (FastAPI uvicorn — public surface, must outlive transient OOM events)
# Lower OOMScoreAdjust than workers so the kernel prefers killing workers first.
[Service]
Slice=grid.slice
MemoryAccounting=yes
MemoryHigh=16G
MemoryMax=24G
TasksMax=8192
OOMScoreAdjust=100
