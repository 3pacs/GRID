# Tier: llm-large (Qwen3-32B, Q4_K_M, full GPU offload on Blackwell)
# Smaller resident than the 49B because Q4 + smaller param count.
[Service]
Slice=grid.slice
MemoryAccounting=yes
MemoryHigh=24G
MemoryMax=32G
TasksMax=4096
OOMScoreAdjust=200
